<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-16 slice 4 — `JOIN <fingerprint>?`, the confirmed join (N3) · dispatch brief · 2026-08-23

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-22-ui16-nearby-onboarding-spec.md` — implement its **N3** section (`:664-686`)
exactly. Its pins are QUOTED here (the standing lesson: a brief quotes a spec rule, never remembers it), but on
any divergence the SPEC governs. ⛔ **NO DEVICE CONTACT.**

## Scope (spec :666-674)
- ONE new `Provision` arm, ONE new `UiProvOp` (`join_team`), ONE new `UiProvOutcome` (`team_joined`) with its
  lexeme, and the adapter arm building `TeamRequest{ mint=false, team_id }`. ⛔ No new seam: `IUiProvision::perform`
  already dispatches `default`-less on `UiProvOp`.
- Files: `src/firmware_ui_model.h` · `src/firmware_ui_prov.h` (the adapter arm, **the same file and the same
  `ITeamCreateDevice` seam**) · `src/firmware_ui.cpp` (rows; forwards only).
- ★ **THE PHY PRECONDITION IS REUSED, NOT RE-SPELLED (F-5, U1)** — `live_phy_matches` against the **persisted**
  record with `present = true`, while the request's own `ProvPhy` stays `present = false` so the transaction
  preserves the persisted PHY and performs **no retune**. ⛔ Two `ProvPhy` objects, never assigned to one another.
- ⛔⛔ **N3 must NOT anticipate K5** (spec §K5): a nearby join of a team whose key is retained in the keyring
  leaves the node **KEYLESS** and the retained record untouched — nothing is installed by knowledge of the public
  team id (P-2b). `SAVED KEY FOUND` / `USE SAVED KEY` are K5's, later.

## Pins (spec :675-681, verbatim)
(1) `BACK` selected initially; reaching `JOIN` costs `short` then `double`. (2) ★★★ the act carries the **FULL
32-bit `team_id` from the row's identity** — ⛔ never the index, ⛔ never re-derived from the token (P-7).
(3) `BACK` returns to the **NEARBY list**, ⛔ not the menu. (4) `phy_differs` refuses with zero transaction calls,
writes, airtime and retunes. (5) Success renders **`TEAM JOINED`** — ⛔ never `TEAM CREATED` (F-4) — plus the full
id and the shared fingerprint. (6) `save_failed` keeps the previous membership and key and says so. (7) ★ after
the join the node is **KEYLESS** (P-2: `team_channel_key_present()` is false) and the screen ⛔ never implies
readership. (8) Terminal, acknowledged by either press, ⛔ no `BACK` row. (9) An unattached seam **fails closed**.
Plus §3's rows: P-1 — the joined `team_id` is **byte-equal** to the observed one.

## Mutations (spec :682-686, `uiprov` + `model`)
The confirmation defaulting to `confirm` · the id from the index · the id re-parsed from the token · ★ **`mint`
left `true`** (a "join" that MINTS a team — the headline control) · `rq.phy.present` set true (a retune, [[B209]])
· the precondition's `persisted` built with `present = false` (the precondition becomes a no-op) · `team_joined`
mapped onto `created`'s string · `save_failed` rendered as success. Both id mutations RED at match count 1.

## Strings (spec §8 — verbatim, no substitutes)
S-8 `JOIN %s?` (`JOIN 3D9348?`) **NEW** · S-9 `JOIN` / `BACK` **REUSED — CALL `join_confirm_label`, don't
re-spell** · S-10 `TEAM JOINED` **NEW**. ⛔ Forbidden anywhere: `KEYLESS` (S-33), `JOIN COMPLETE`,
`TEAM CREATED` on this path. A lexeme the spec lacks ⇒ STOP and report.

## Operational contract (standing)
- C1: N3 only. Everything landed survives (S1-S8, K1/K2, N1, N2 — batteries, probes, the UI-17 contract
  mutations; N2's zero-TX rule extends: the CONFIRMATION screen itself transmits nothing — only a confirmed
  `double` reaches the transaction).
- The handoff seam WITH the unit: drive a confirmed join through the REAL adapter/service on the probe, plus the
  `PHY DIFFERS` and BACK-performs-nothing arms; controls RED.
- Batteries: iterate; ONE full pass per touched target (`uiprov`, `model`, any other touched); sync `PIN_*` on
  moves with derivation; exit 6/7/8 = fix the invocation; never probe+battery concurrently; runner-marker waits;
  ⛔ never edit a battery's target while it runs (the N2 incident).
- Resources: spec §6 N3 row says "likely 0 — **Measure both**" (`UiProvIntent`'s u32, the enumerator) — measure
  and report.
- Verification you run (QG runs boards/corpus — N3 is `src/`-only, s18-inert by construction; say so): native
  (RUN the binary) · touched-target batteries · `probe_firmware_ui` both arms all controls · `git diff --check`
  clean · `git diff -- lib/` shows NOTHING beyond what `git status` already carries from prior slices (N3
  touches no `lib/` file).
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, either UI spec, the design doc, `tracker.md`, `docs/manual/`, or anything under `lib/`. The metal
  residue (spec §7.2's joiner steps): DRAFT in your report (supervisor-landed on PASS).

## Report
Every pin with case name and match count · the handoff arms with controls RED · the keyless proof
(`team_channel_key_present()` false after a real seam join, and the retained-key non-anticipation case) · the
mutation ledger + full-pass proofs · native counts + cross-check with derivation · probe proofs · measured
resources · the DRAFTED §7.2 bench residue · exact final `git status --short`.
