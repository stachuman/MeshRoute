<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-17 S4 — TEAM row format · dispatch brief · 2026-08-21

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-20-ui17-navigation-status-team-redesign-spec.md` — implement its **S4** slice
exactly: the ruled row format in the new pure `firmware_ui_team.h`, the label resolver REUSED (never re-spelled),
last-heard as ROUTE/KNOWN age (⛔ never "heard" language), DIST/DIR columns present but BLANK until S5's
projection, HOPS and `score_q4` dropped BY RULING (the design doc already records the removal), and **the bounded
clock-driven repaint that closes the pre-existing F-8 staleness gap** (a lit TEAM screen's ages must turn — via
the §CHROME-3 invalidation idiom, ⛔ no new timer, the throttle still free to refuse). ⛔ **NO DEVICE CONTACT.**
Build on the current tree (S1-S3 + harness QG-passed); revert nothing.

## Two lessons from S3, IN SCOPE from the start
1. ★★ **The production-handoff seam is covered on day one:** S4 has the identical pure-unit → renderer seam S3's
   QG HOLD exposed. Drive distinctive values through the REAL renderer (the P17 pattern — real node, exact text +
   coordinates per row, no `settle()` where navigation would change the screen), with drop/misroute controls per
   row class. Pure-helper mutations alone are insufficient — plan the handoff checks WITH the unit, not after.
2. ★ **[[B229]]'s residue closes in this slice** (its row's close-by names "the next probe-touching slice", and
   two slices have deferred it): either shorten the four >64-byte labels (`P14d precondition…`, `P6d ★…`,
   `P6d ★★…`, `P7b …withdrawn marker TEXT`) or make the roll-up's label parse length-proof — your choice, stated;
   prove the previously-unattributable C84 now attributes. Separate attribution in the report (it is B229's
   closure evidence, not S4's).

## Operational contract (as S3's)
- ⛔ S4 ONLY (C1) + the B229 closure above. S1-S3 behaviours/mutations survive (entries RED, cases GREEN — incl.
  S3's P17 phase and the frozen-frame rule: TEAM rows render from the FROZEN snapshot only, no live reads).
- Batteries: iterate on changed entries; ONE full pass of every touched target before reporting; a new pure
  header gets its own target; sync `PIN_*` on count moves (derivation in place); exit 6/7/8 = fix the invocation;
  never probe+battery concurrently; wait-loops on runner markers.
- Strings/format: the spec's ruled row format and inventory govern; a lexeme or format case the spec lacks ⇒
  STOP and report. Spec-vs-tree conflict the spec does not resolve ⇒ STOP and report.
- Resources: measured per the spec's plan (TeamRow deltas belong to S5 — S4 should be near-zero; measure anyway).
- Verification you run (QG runs the full gate): native (RUN the binary) · touched-target batteries (full pass) ·
  the probe both arms, all controls, no stray output · `git diff --check` clean.
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, the design doc, the SPEC, `tracker.md`, `B164.md`, `docs/manual/`, or anything under `lib/`.
  Doc corrections S4 forces: DRAFT in the report.

## Report
Every S4 pin with case name and match count · the handoff checks (exact expected rows) with their controls RED ·
the F-8 repaint fix and its counted proof (ages turn on a lit screen; the throttle still refuses; sleep
unaffected) · the B229 closure evidence, separately attributed · mutation ledger + full-pass proof · native
counts + cross-check sync · probe proofs · measured resources · any drafted doc text · exact final
`git status --short`.
