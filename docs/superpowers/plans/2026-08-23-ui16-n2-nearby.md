<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-16 slice 3 — the NEARBY list (N2) · dispatch brief · 2026-08-23

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-22-ui16-nearby-onboarding-spec.md` — implement its **N2** slice exactly, under
the folded rulings: **OQ-1** (`JOIN TEAM` opens NEARBY directly — one row in the PROVISION child menu, no
submenu) · **OQ-4** (signal as `0/3…3/3` from the EXISTING `presence_quality_tier()` — ⛔ a second definition of
signal quality is the named forbidden shape, with its mutation) · **OQ-5** (first-observed order — the list
renders the ring's structural order, ⛔ never signal-sorted) · **R-13 rule 1** (rows identified by the TEAM
fingerprint via THE shared helper; ⛔ an advertiser's NODE name never presented as the team name — its control
goes RED) · frozen-snapshot-per-entry with MANUAL refresh only · **the own/current team recorded by N1 and
FILTERED HERE at display** (the spec's rule — N1 records like any other; N2 filters). ⛔ **NO DEVICE CONTACT.**

## Scope (N2 per the spec; its pins/mutations govern)
- The `JOIN TEAM` child in the PROVISION menu (the landed §6 hiding rules and `provision_has_child` extend —
  reconcile with the landed model exactly as the spec states) → the read-only NEARBY list: fingerprint ·
  signal tier · age per row, `BACK` contained, the UI-17 navigation contract binding (passive/enter/BACK,
  retention across blank/wake).
- The projection runs at `build_snapshot` over **N1's const accessors**; the renderer reads ONLY the frozen
  snapshot (the S3 tear class must not return). CAN ONLY LOOK: zero TX, zero writes — the probe's zero-TX
  assertion is the slice's own check (spec §7.1's scheduled bench check gets drafted this slice).
- The handoff seam covered WITH the unit (the standing lesson): distinctive cache entries through the real
  node → exact rows at exact coordinates, drop/misroute/sort/name-as-team controls.
- Strings: the spec's inventory verbatim (S-6 as amended, the empty-state lexeme, S-7's tier tokens); a lexeme
  it lacks ⇒ STOP and report.
- The F-1 design-doc correction is scheduled at N2 by the spec — **DRAFT it in your report** (the design doc is
  supervisor/QG-landed); same for the §7.1 bench check text (M2, supervisor-landed on PASS).

## Operational contract (standing)
- C1: N2 only. Everything landed survives (S1-S8, K1/K2, N1 — batteries and probes; the UI-17 contract's
  mutations must stay RED through the new child).
- Batteries: iterate; ONE full pass per touched target; a new pure header gets its own target; sync `PIN_*` on
  moves; exit 6/7/8 = fix the invocation; never probe+battery concurrently; wait-loops on runner markers.
- Verification you run (QG runs boards/corpus — N2 is `src/`-only, s18-inert by construction; say so): native
  (RUN the binary) · touched-target batteries · `probe_firmware_ui` both arms all controls (the new phase +
  the zero-TX negative) · `git diff --check` clean · `git diff -- lib/` EMPTY.
- Resources measured (snapshot growth for the projection rows — the spec's §6 estimate vs measured, the
  read-off-the-image discipline).
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, either spec, the design doc, `tracker.md`, `docs/manual/`, or anything under `lib/`.

## Report
Every N2 pin with case name and match count · the handoff checks (exact rows) with controls RED · the zero-TX
proof · mutation ledger + full-pass proofs · native counts + cross-check · probe proofs · measured resources ·
the DRAFTED F-1 design-doc correction and §7.1 bench text · exact final `git status --short`.
