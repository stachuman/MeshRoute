<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-10/11 slice P2 — the `ui preset` verbs, NDJSON and the live wiring · dispatch brief · 2026-08-25

**Status: DISPATCHED (P1 QG-passed).** Authorities: the arc spec
`docs/superpowers/specs/2026-08-25-ui10-11-preset-catalog-spec.md` (§2 rulings — the `busy` table, `PRESET
CHANGED` is P3's, the documentation rule; §3-P2 row) and the parent design's **§3.2.3 grammar + NDJSON records
verbatim** (`docs/superpowers/specs/2026-07-31-onboard-oled-ui-design.md:412+`). P1's landed service
(`src/firmware_ui_presets.h`, QG-passed) is consumed, ⛔ not modified except where a P2 seam genuinely needs a
hook (say which and why). ⛔ **NO DEVICE CONTACT. No UI/compose change (P3's).**

## Scope
- **The verb family, grammar EXACTLY as §3.2.3 spells it:**
  `ui preset list` · `ui preset set <emergency|dm1..dm8|channel1..channel8> loc=<on|off> "<text>"` ·
  `ui preset clear <dm1..dm8|channel1..channel8>` · `ui preset reset <emergency|dm1..8|channel1..8|all>`.
  Through the ONE command dispatch (the consolidated `dispatch(line,len,Print&)` — three transports, one
  path; ⛔ never a BLE fork). The `loc=` parser is **`bad_location`'s producer** (P1 declared the reason with
  no producer — this slice closes that done-vs-missing marker).
- **The three NDJSON records verbatim** (parent §3.2.3): `ui_preset` per slot · `ui_presets_end` with
  `capacity:17`, actives, generation · `ui_preset_err` with the SIX reasons. `list` emits all 17 in stable
  slot order incl. disabled, then the end record. Mutating verbs return the resulting record; `reset all`
  returns the full list. Exact JSON natively pinned.
- **The `busy` table from spec §2 exactly** (active emergency ⇒ every mutating verb `busy` incl. no-ops; the
  candidate-with-next-generation → save → publish order is P1's, already proven — the verbs only route).
  ⓘ The modal-close half is P3's: P2 exposes/threads whatever fact P3 will need, ⛔ without touching compose.
- **The live wiring (P2 pays the resident cost):** ONE `PresetCatalog` instance (placement per P1's ruled
  no-stack decision), loaded at boot through the four-state read, the two EXACT diagnostic lines printed from
  P1's `preset_boot_line()` (`nullptr` ⇒ no line), `status`/`cfg` surfacing per the design. Factory reset
  already erases `/mrui` via the `"mr"` namespace — verify, don't re-implement.
- **Console help is CODE and yours**: the hierarchical help gains the verb family. **The two DOCUMENT targets
  are DRAFTED in your report** (supervisor lands on PASS): the `ios-companion/INBOX_SYNC_CONTRACT.md` events
  addition and the `docs/manual/command-reference.md` section — exact proposed text, not summaries.

## Pins (the P2-reachable set from spec §4 + the round's own)
(10) the three NDJSON records exact — every field, every reason spelling, natively pinned byte-for-byte.
`list` = 17 records + end, stable order, disabled included. Mutating verbs return record / full list. The
`busy` table row by row (incl. a no-op `set` during an active emergency ⇒ `busy`, zero loads/writes). The
boot lines: each of the four storage states drives the right line (or none) through the REAL boot path on the
probe. `bad_location`'s producer: `loc=maybe` ⇒ the reason, zero writes. USB and BLE byte-agree (the one-
dispatch proof). Board RAM: the +~1.1 KB lands here — measure the native delta; the per-board figure is QG's.
- Mutations (each RED at match count 1): a reason mis-spelled/collapsed · `list` skipping disabled slots ·
  the end record's capacity/generation dropped · a mutating verb answering with a dump instead of the record ·
  the BLE path forked from the USB path · `busy` bypassed for a no-op · the boot line printed for `ok`/
  `absent` · `loc=` accepting a third value.

## Operational contract (standing)
- C1: P2 only. P1's batteries/cases survive byte-identical (`uipresets` untouched unless a hook forces it —
  say which). Parallel runner per its header; iterate; ONE full pass per touched target (`uipresets` if
  touched, `config`/`consolejson`-class targets as they exist — check `TARGET_SRC`); PIN derivation written;
  ⛔ never pipe; ⛔ no pollers; ⛔ never edit a target mid-run.
- Handoff seam WITH the unit: the probe drives the verbs over the REAL dispatch + REAL store fake through the
  real boot path (the four boot lines, the list, a set→reboot-shaped reload); controls RED.
  ⛔ **AMENDED 2026-08-26 (P2 QG — the first cut satisfied this with a WEAKER FAKE, and three real-wiring
  removals stayed green):** the production WIRING itself must be gated — the live `ui` dispatch arm
  (`firmware_commands.cpp`), the boot-restore call (`fw_main.cpp:885`), and the no-BLE-fork fact
  (`fw_main.cpp:547` area) each need a RED control. The full-device-TU link is impractical host-side (those
  TUs need Arduino/RadioLib — first compile is the boards'), so the ruled mechanism is the **structural wiring
  battery in `tools/probe_board_ui`** (the W47/W48 precedent: source pins at match count 1 with sed-mutation
  controls on the real files) — **a DOCUMENTED RELAXATION of this plan's original wording, recorded here per
  QG's ruling**, stated in the harness at the checks.
- Verification you run (QG runs the two-env boards — ⚠ `firmware_config.cpp`-class TUs first-compile there;
  say so if touched): native (RUN the binary) · touched-target batteries · both probes · `git diff --check`
  clean · `git diff -- lib/` EMPTY ⇒ s18-inert by construction (state it).
- ⛔ NEVER `git commit`/`git add`/`git checkout --`. ⛔ No docs edits (the two targets are DRAFTS in the
  report; console help is code and allowed), no tracker, no platformio.ini, no parallel-session files. Metal
  residue additions (the verbs over real USB + BLE): DRAFT in your report.

## Report
Every pin with case name and match count · the exact NDJSON pinned bytes · the busy-table proofs · the boot-
line four-state probe proof · the one-dispatch USB/BLE agreement proof · the measured resident delta · the
mutation ledger + full passes · native + PIN derivation · the TWO drafted document texts (exact) · the metal
draft · exact final `git status --short`.
