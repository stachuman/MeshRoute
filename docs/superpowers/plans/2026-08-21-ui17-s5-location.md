<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-17 S5 — location projection · dispatch brief · 2026-08-21

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-20-ui17-navigation-status-team-redesign-spec.md` — implement its **S5** slice as
CORRECTED by the §3.4 pull-forward note: **the OWN-location snapshot fields are ALREADY DONE (S3)** — S5's whole
scope is the PEER side: the `TeamRow` peer-location fields, the projection over the EXISTING authenticated
`peer_loc_find` cache, and the pure geo unit the spec names (`firmware_ui_geo.h`: freshness **600 s**, int64
longitude, the antimeridian fold, the atan2-free octant; **coincident points ⇒ `0m` + BLANK direction,
`has_bearing = false`, ⛔ never octant 0** — ruled). The DIST/DIR columns S4 reserved BLANK now fill — **only**
when our fix AND the peer's cached position exist AND the cached position is within its freshness bound; a stale
fix renders BLANK, ⛔ never a plausible number (C2). ⛔ **ZERO wire, ZERO new traffic** — the projection reads the
cache; nothing requests, broadcasts or refreshes a location. ⛔ **NO DEVICE CONTACT.** Build on the current tree
(S1-S4 + B217/B229/B235/B237 all QG-passed); revert nothing.

## Carried-in obligations
1. The S5 line S4 queued: add to `TeamRow`'s struct comment —
   `// ⛔ hops / score_q4 are WRITTEN AND READ BY NOTHING since §UI-17 S4 (spec §1.9 F-1/F-2) — deleting them is
   their own refactor slice (C1).` (You touch `firmware_ui_model.h` anyway for the peer fields.)
2. **Resource plan:** the spec's §6 row (RE-BASED: `UiSnapshot` 616 → ~744 est., `TeamRow` 28 → ~44 est.) —
   **MEASURE both** (the reveal-command idiom), record measured vs estimate, sync the spec's figure via a drafted
   correction in your report (⛔ the spec is not yours to edit).
3. The production-handoff seam covered WITH the unit (the S3/S4 lesson): distinctive coordinates through the real
   node's cache (`peer_loc_set` — the core's own seam), exact DIST/DIR text at exact coordinates, drop/misroute/
   stale-not-blank controls. The geo maths get their own battery target; the freshness/blank decisions are pure
   and mutation-covered (fresh↔stale boundary, the 600 s edge, both-fixes-required, the fold, the octants).

## Operational contract (as S4's)
- ⛔ S5 ONLY (C1) + obligation 1. S1-S4 behaviours/mutations survive (frozen-frame rule included: the projection
  runs at `build_snapshot`, the renderer reads only the frozen rows — the S3 tear class must not return one
  struct over).
- Batteries: iterate; ONE full pass per touched target; new pure header ⇒ own target; sync `PIN_*` on moves;
  exit 6/7/8 = fix the invocation; never probe+battery concurrently; wait-loops on runner markers.
- The spec's string inventory governs (the DIST/DIR formats are ruled — `1.2k`/`340m`-class tokens and the octant
  letters; verify against the spec's §3.4/§8, not memory). Missing lexeme / unresolved conflict ⇒ STOP and report.
- Verification you run (QG runs the full gate): native (RUN the binary) · touched-target batteries · the probe
  both arms all controls (now under the B237-hardened runner — an `abnormal`/`silent` verdict is yours to fix) ·
  `git diff --check` clean.
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, the design doc, the SPEC, `tracker.md`, `B164.md`, `docs/manual/`, or anything under `lib/`
  (reading `node_hashlocate.cpp` is expected; writing it is not). Doc corrections: DRAFT in the report.

## Report
Every S5 pin with case name and match count · the handoff checks (exact rows incl. a stale-cache BLANK case) with
controls RED · the geo unit's boundary cases (600 s edge · antimeridian · coincident `0m`+blank · octants) ·
mutation ledger + full-pass proofs · native counts + cross-check sync · probe proofs · MEASURED `UiSnapshot`/
`TeamRow` vs the spec's estimates, with the drafted spec-figure correction · any other drafted doc text · exact
final `git status --short`.
