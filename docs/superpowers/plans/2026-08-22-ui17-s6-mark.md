<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-17 S6 — the MeshRoute mark (interim "MR") · dispatch brief · 2026-08-22

**Status: QUEUED behind S8 (probe-file collision); the APPROVED spec's S6 is the AUTHORITY, narrowed by an OWNER
RULING 2026-08-22 (reported form): the asset landed NOW is an INTERIM mark — a simple 24×24 monochrome bitmap
rendering the letters "MR" — and the FINAL artwork arrives later from the owner as a pure byte-swap.** S6 may
therefore run twice; both runs are asset-only by the spec's own rule.

## Scope (asset-only — the spec's S6)
1. A native **24×24 monochrome XBM** (72 bytes) drawing a legible "MR" letterform, in `src/firmware_ui_icons.h`
   beside the existing assets, in the repository's established XBM bit order, with the byte-order contract the
   file's other assets carry. ⛔ No runtime scaling; ⛔ no geometry change: the slot stays `x=12..35, y=12..35`
   and replacing the asset later must not move a single text row (the note's permanent-reservation rule).
2. `draw_status_screen`'s `draw_rect` placeholder → the one `draw_bitmap` call through the existing seam.
3. In-source: the asset is marked **INTERIM (owner ruling 2026-08-22) — the final mark replaces these 72 bytes
   and nothing else**.
4. Probe: the STATUS census in P14a/P17 moves (the placeholder rect leaves, a bitmap arrives —
   `body_rects_on_page(0)` 1 → 0, `bitmaps_on_page(0)` 12 → 13 on STATUS, or whatever the measured truth is —
   assert the MARK's exact rect via the bitmap record as the rect was asserted before; never weaken). The
   `icons` battery target covers the new asset per that file's existing pattern (the byte-order contract case).
5. Resources: the 72 B `.rodata` measured (the spec's §6 note: the placeholder cost 0, the asset is the 72 B).

## Operational contract (as recent slices, abbreviated — the standing rules all apply)
S6 only (C1) · S1-S5 + S8 survive · full pass per touched target (`icons` at least) · PIN sync on moves · probe
both arms, no abnormal/silent · `git diff --check` clean · never probe+battery concurrently · no commits, no
register/bench/plan/spec/design-doc/`tracker.md`/`B164.md`/`docs/manual/`/`lib/` edits · drafted doc text in the
report.

## Report
The XBM bytes' provenance (how the glyph was authored) and a rendered ASCII proof of the 24×24 · the census
re-points · the battery + probe proofs · measured `.rodata` delta · native counts · exact final
`git status --short`.
