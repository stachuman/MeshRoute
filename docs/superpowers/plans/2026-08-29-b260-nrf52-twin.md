<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §B260 — the nRF52 inbox twin's durable-ack defects · dispatch brief · 2026-08-29

**Status: DISPATCHED — ★ owner-ruled 2026-08-29 ("B260 should be fixed"), inserted before custody Slice A.**
Authority: the register row **[[B260]]** (`docs/2026-07-30-open-bug-register.md` — read it in full: FIVE defect
lines, all found by [[B134]]'s QG rounds, all listed in-source at the top of `src/device_inbox_store.h`).
The B134-hardened shared logic (`lib/core/segmented_inbox_store.h` + the seam pattern
`src/device_inbox_fs_esp32.h`) is the reference implementation — its eight QG rounds are the spec of what
"durable" means here. ⛔ NO DEVICE CONTACT. ⛔ Custody Slice A is the NEXT stage, separate.

## The defects (the row's five lines — fix ALL)
① every `save_meta()` result ignored (rotation = acknowledged-record loss + a returning tombstoned message;
eviction; begin's upgrade/§10.1 saves; the fresh branch persisting nothing = seq reuse) ② unchecked
`qspi_seg_erase` in append's roll/eviction + `wipe()` leaving RAM ring state stale ③ `!had_meta`
re-initialising over existing QSPI records ④ the every-boot §10.1 epoch ratchet ⑤ `set_read_cursor`
RAM-first + change-detect (unrepairable failed save) and `set_next_seq` over-reporting.

## The shape — ★ QG-PINNED 2026-08-29 (brief-review verdict: architecture GO, retirement not the hand-port)
**RETIRE the hand-maintained twin.** The pinned form: an **nRF52-only adapter** implementing `ISegmentStore`
and `IMetaStore`, while **`SegmentedInboxStore` is reused UNCHANGED**. ⛔ Do NOT include the ESP32-specific
header (`device_inbox_fs_esp32.h`) and do NOT copy its ring logic — the adapter is a sibling seam, not a
second consumer of the ESP32 one. The adapter must **check raw LittleFS seek/write/sync/close results**
(verify what CustomLFS/InternalFS calls ACTUALLY report on nRF52 — the Arduino-void-`flush()` lesson, held to
the readdir_r evidence standard) and **distinguish absent files from inspection errors** (three-valued, the
D6/D7 precedent). ⛔ The fallback hand-port is OFF the table per the review; if the mapping genuinely
contorts, STOP and report — don't improvise.
- **★ Migration is DESTRUCTIVE, definitely, not conditionally** (QG measured it: the existing nRF52 meta is
  24 B, the shared meta is 28 B with `records_state` ⇒ the old blob classifies `meta_corrupt` on first boot).
  ⚠ **`factory_reset confirm` wipes CONFIG, IDENTITY, PEERS, TEAM STATE and the inbox — not only inbox
  data.** Accepted ONCE under M3 for development devices — owner-endorsed via the review; ⛔ NO automatic
  reinterpretation of old bytes. The refusal arm + the recovery are both tested; the full-wipe consequence is
  stated wherever the migration is described (report, drafted bench step, drafted register text).
- **★ Fail-loud diagnostics parity**: the nRF52 boot line reports only `enabled=` today while ESP32 reports
  `epoch=` and both `mount_fault=` values. After adoption nRF52 MUST expose the same information — otherwise
  `meta_corrupt`, `meta_lost_over_records` and inspection failures are operationally invisible on the
  platform this slice exists to fix.
- The B134 semantics come along COMPLETE via the reused store — checked saves w/ rollback+latch, fallible
  commit, observable wipe, the fresh-vs-torn discriminator, three-valued meta + marker validation, the
  transition-driven epoch, mark_read/next_seq verdicts. The verbs' conditional output already landed
  platform-shared in B134 — verify it binds here.
- **★ Stale-reference sweep (QG-required)**: once the twin is retired, remove `DeviceInboxStore` type
  assumptions from `fw_context.h`, `fw_main.cpp`, comments and probes — list every site swept.

## Gate
- Native: the shared-store suite must pass UNCHANGED; new cases for whatever nRF52-specific decision logic
  exists after the reshape (pure/hoisted per the D5/D6/D7 precedent — the QSPI raw arm will be
  host-unreachable like LfsIo's; keep the decisions above the `#if`, document the residue honestly).
  Mutations: extend the isolated-harness targets (a `b260` target set per touched file; the B134 arms
  re-aimed where the code is shared vs twin — if retirement lands, most B134 mutations already cover it and
  the NEW target attacks only the nRF52 IoT/meta adapters). Full pass per touched target, PIN derivation.
- Corpus: state precisely what sim-compiled code moves (likely none beyond what B134 already moved — verify);
  lus md5 vs the current reference w/ the rebuild control; s18 tripwire; the scratch-BEFORE/pristine-baseline
  A/B method if anything moves.
- Boards: the ruled pair — ⚠ this slice TARGETS `gateway` (nRF52): RAM/flash measured + attributed (the
  certified runner if material); `heltec_mobile` expected BEHAVIOUR-UNCHANGED (state the guard). ABI probe
  for any touched pinned struct. Warning census; `git diff --check`.
- ⛔ NEVER `git commit`/`git add`/`git checkout --`. ⛔ No docs (register/spec/bench drafts in the REPORT),
  no `tracker.md`, no `platformio.ini`, no parallel-session files. No pollers; never pipe the runner.
- Metal residue is REAL (QSPI faults are host-unreachable): DRAFT the nRF52 bench steps mirroring the B134
  set (power-cut mid-write, corrupted-meta refusal, exactly-once epoch, destructive verbs, the one-time
  old-format refusal incl. its FULL-WIPE consequence) with exact expected console lines.
- **★ Closure status (QG-defined)**: host tests + board builds make B260 SOFTWARE-COMPLETE only — the row
  stays **METAL-PENDING** until the nRF52 power-cut, corruption, epoch, wipe and migration-reset checks pass
  on the bench. The drafted register text must say so.

## Report
Phase 0: the five defects V1-confirmed (file:line) + the mapping measurement + the shape decision argued ·
the reshape/port with the reuse boundary stated · every B134 semantic accounted for (a checklist against the
eight rounds) · migration behaviour + its tests · mutation ledger + native PIN · corpus argument/result ·
board measurements attributed (gateway is the subject) · DRAFTED doc corrections + bench steps · exact final
`git status --short`.
