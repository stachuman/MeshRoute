<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-16 slice 7 — `GRANT KEY` / `REJECT` with `send_aired` correlation (N6) · dispatch brief · 2026-08-24

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-22-ui16-nearby-onboarding-spec.md` **§N6 (:808-849 area — locate the
`### N6` heading)** — read it IN FULL; this brief quotes its load-bearing rules but the SPEC governs. Also read
§N5's preflight clarification blockquote (the equivalence-case obligation lands HERE), the §8 entries S-17,
S-22/S-23, **S-24** (the six remaining `TeamKeyGrantTx` arms, one line each, ⛔ they may not collapse) and
forbidden S-32/S-33/S-34, and the §3 rows P-7c/P-7d/P-8. ⛔ **NO DEVICE CONTACT.**

## Scope (spec, verbatim)
The two actions with **`REJECT` selected initially**, the confirmation, ONE adapter forward to
`Node::team_key_grant_send` **with `Plane::TEAM`**, and the **`send_aired` correlation**. ⛔ No new send path, no
new payload, no new frame type, no wire byte. Files: `src/firmware_ui_invite.h` (the mapping + the correlation
rule, **pure**, all eight arms + both push outcomes natively drivable) · `src/firmware_ui_model.h` (arms + op) ·
a device forward · `src/firmware_ui_send.h` (the `send_aired` arm — already the ONE pure push router).

## ★★★ The outcome mapping IS the slice (F-9 ruled, spec verbatim)
⛔ WITHDRAWN, KEPT VISIBLE: *"`ctr != 0` = airborne ⇒ `KEY SENT`"*.
- **`queued` with `ctr != 0` ⇒ `GRANT QUEUED`** (admitted to the queue, nothing has left the radio);
- **a CORRELATED `PushKind::send_aired{dst, ctr}` ⇒ `KEY SENT`** (`lib/core/command.h:246` — the TxDone edge for
  *this* flight);
- **a correlated failure ⇒ `GRANT FAILED`**;
- **`queued` with `ctr == 0` ⇒ PARKED behind an H resolve** (`src/firmware_config.cpp:1370-1371`) and says so in
  its own words;
- ⛔ **no e2e ack exists on a grant** (`lib/core/node_mac_rx.cpp:1723`) ⇒ ⛔ never `JOIN COMPLETE`, never
  `KEY RECEIVED` here.
- ⓘ `delegated` is unreachable on the real seam (the UI always sends `Plane::TEAM`) — ⛔ but the pure mapper
  **FAILS LOUDLY** if a fake returns it, and that arm is DRIVEN (C2).
- ★ The correlation is **`{dst, ctr}` and both terms are load-bearing** — a `ctr` alone is a LOCAL handle and
  the same value legitimately names another flight (`command.h`'s own warning).

## Pins (spec, verbatim — every one a case)
(1) `REJECT` selected initially; reaching `GRANT KEY` costs `short` then `double`. (2) `REJECT` sends nothing,
changes no core/radio/membership/key/NV state — ★ and adds the hash to the handled set (F-13). (3) All **eight**
arms driven, each with its own word. (4) ★ **`GRANT QUEUED` is not `KEY SENT`** — the headline control.
(5) `KEY SENT` requires a **correlated** `send_aired`; an uncorrelated one (different `dst`, or different `ctr`)
⛔ does not promote it. (6) ⛔ No arm prints a completion word. (7) ⛔ No key material reaches any screen (P-8).
(8) The grant is unreachable with the window closed. (9) Terminal, acknowledged by either press. (10) the
`GRANT KEY` and `REJECT` confirmations carry the **full `0x%08lX` hash**, ⛔ even when a name is shown (P-7c).
(11) the grant target is the **`key_hash32`** and ⛔ never the display name; a member whose name changes between
the row and the confirmation is still granted **the same key** (P-7d).
**(12) OWED FROM THE N5 PREFLIGHT RULING (spec N5 Files blockquote):** the equivalence case — ONE fixture drives
`invite_grant_preflight` AND the real `team_key_grant_send` at `authoritative` and one notch below it, proving
the two sites answer identically at the boundary.

## Mutations (spec, verbatim — `uiinvite` + `uisend` + `model`, each RED at match count 1)
★ **`queued` mapped straight to `KEY SENT`** (the F-9 defect restored — the headline) · the correlation dropped
to `ctr` alone · the correlation dropped entirely (any `send_aired` promotes) · the eight arms collapsed to
`ok`/`failed` · `delegated` returning a plausible word instead of failing loudly · `REJECT` calling the send ·
`REJECT` not adding to the handled set · any arm printing a completion word · the plane changed away from
`Plane::TEAM` (⇒ `delegated` becomes reachable) · ★ the grant target taken from the **display name** (P-7d) ·
the confirmation dropping the full hash when a name is present (P-7c) · the handled set keyed by the **name**
(⇒ rejecting one member silences another that shares its name).

## Probe (spec, verbatim)
An INVITE→GRANT arm against a **fake** grant device returning each of the eight outcomes plus both push
outcomes, asserting the exact panel line for each; ⛔ the fake is the seam, never a hard-wired string. Plus the
handoff seam WITH the unit: one real-seam confirmed grant on the probe's real node (H2-shaped fixture with a
cached authoritative pubkey) asserting `GRANT QUEUED`→`KEY SENT` on the real TxDone edge.

## Strings (spec §8 — verbatim; a lexeme the spec lacks ⇒ STOP)
S-17 `GRANT KEY`/`REJECT` (REJECT default) · S-22/S-23 (the queued/sent/failed words as inventoried — read the
rows) · **S-24**: `NO TEAM KEY` · `NO IDENTITY` · `NOT IN A TEAM` · `SELF` · `NAME TOO LONG` · `WRONG PLANE`,
one line each, ⛔ never collapsed. ⛔ Forbidden: `JOIN COMPLETE`, `KEYLESS`, `WAITING FOR KEY`, any completion
word on any arm.

## Operational contract (standing)
- C1: N6 only. Everything landed survives (through N5 + its QG fix + §CHROME-5). The one transmission this slice
  may cause is the operator-confirmed grant; `REJECT`/BACK/close transmit nothing (counted).
- Batteries: iterate; ONE full pass per touched target; sync `PIN_*` with derivation; exit 6/7/8 = fix the
  invocation; never probe+battery concurrently; ⛔ NO background poller shells outliving your final report —
  foreground waits on runner markers only; ⛔ never edit a battery's target while its battery runs. ⚠ A parallel
  session's BOARD build can wipe `.pio/build/native` mid-battery — if a battery dies FileNotFoundError, verify
  the restore, rebuild native, re-run that battery IN FULL.
- Resources: measure any growth (offsetof-prove placements).
- Verification you run (QG runs boards/corpus — N6 is `src/`-only, s18-inert by construction; say so): native
  (RUN the binary) · touched-target batteries · probe both arms all controls · `git diff --check` clean · diff
  stays `src/` + `test/` + `tools/`.
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, either UI spec, the design doc, `tracker.md`, `docs/manual/`, anything under `lib/`, or the
  parallel-session files. The metal residue (spec §7.4 steps 3c-6's grant half — the real sealed grant over real
  air): DRAFT in your report.

## Report
Every pin (1-12) with case name and match count · all eight arms + both pushes with their exact words · the
correlation proofs (correlated promotes; different-dst and different-ctr do not) · the equivalence case ·
the mutation ledger + full-pass proofs · native counts + cross-check with derivation · probe proofs · measured
resources · the DRAFTED bench residue · exact final `git status --short`.
