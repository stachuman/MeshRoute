<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §B20/B21 — the silent DM length boundary · dispatch brief · 2026-08-28

**Status: DISPATCHED (the owner-ruled pipeline's message-correctness stage).** Authority: the register rows
**[[B20]]** ("Encrypted DM lengths 215-216 can disappear without `send_failed`" — OPEN/HIGH) and **[[B21]]**
("Oversized DM reports the wrong condition and no `send_failed`; investigate with B20 but retain distinct
causes" — OPEN/HIGH), `docs/2026-07-30-open-bug-register.md:84-85`. ⛔⛔ **THIS IS A `lib/core` SLICE — the
corpus obligations are not optional.** ⛔ NO DEVICE CONTACT. ⛔ B159/B134/custody are later stages.

## Phase 0 — REPRODUCE AND MAP before any fix (the non-negotiable half)
- Build the **exact boundary matrix** natively, driven through the PUBLIC command path (never internals):
  for each carrier variant that changes the cap — plaintext DM · CRYPTED DM · ±`SOURCE_HASH` · ±location ·
  ±`E2E_ACK_REQ` · delegated/`SEALED_RELAY` where reachable — walk the body lengths across the boundary
  (cap−1, cap, cap+1, cap+2, …) and record for EACH length: the synchronous `CmdCode` · any async
  `SendFailReason` push · whether ANYTHING airs (the real HAL queue/radio counters, the N2 zero-TX idiom) ·
  whether the sender is left believing success. ⓘ Known reference points to verify, not assume: the direct
  sealed body cap is documented as 214 (208 located) in the GNSS spec's §8.3, and the generic command cap
  as 239 — the 215-216 window in B20's row sits exactly between a carrier cap and a laxer upstream check;
  find BOTH checks in the source and name them (V1, `file:line`).
- From the matrix, state **B20's cause and B21's cause SEPARATELY** (the row demands distinct causes — do
  not merge them into one boundary off-by-N without proving they are one).
- The reproductions become permanent RED-before-fix cases (the B253 idiom): the silent-disappearance case
  (something accepted, nothing airs, no failure ever surfaces) and the wrong-condition case (the code the
  sender sees vs the condition that actually held).

## Phase 1 — the fix (C2, fail-loud, one length authority)
- The refusal fires at the EXACT carrier boundary, preflight-before-mint where the current path can (the
  established discipline: no counter burned, nothing queued, no partial state), with the true condition:
  `err_too_large`/`SendFailReason::too_large` for a genuinely oversized body; whatever B21's misreported arm
  is becomes its true code. ⛔ No silent truncation, ⛔ no silently-dropped queued item, ⛔ no new lexeme/code
  unless no existing one is TRUE (then STOP and report the candidate).
- **One length authority**: the existing packers/carrier formulas (the SEALED_RELAY/§8.3 "the packers are the
  one length authority" rule) — ⛔ never a second hand-written cap table. If two checks exist (carrier cap +
  the 239 generic), reconcile them so the STRICTER true bound governs and the laxer one cannot admit what
  the carrier will drop; document which check owns what.
- Every length in the matrix lands one of exactly two honest outcomes: it AIRS and delivers, or it REFUSES
  loudly with the true condition. Nothing in between survives.

## Tests / mutations / gates
- The full boundary matrix as native cases, both sides of every variant's cap (the UI-10 §8.3 both-sides
  idiom); the two RED-before-fix reproductions flipped; e2e delivery proven at cap and refusal at cap+1 for
  each variant.
- Mutations (new/extended `lib/core` battery targets per the b161/b251 precedent, each RED at match count 1):
  the boundary check dropped · off-by-one (≤→<) · the laxer check re-admitted past the carrier cap · B21's
  wrong condition restored · the silent-drop path re-opened (accepted-but-never-airs) · the refusal burning
  a counter/queue slot.
- **Corpus obligations (lib/core):** the s18-inertness ARGUMENT stated precisely (a refusal path for lengths
  that previously disappeared — do any corpus scenarios send 215-216-byte DMs? MEASURE the corpus occurrence
  of the affected lengths in Phase 0; if any stream can move, predict WHERE before QG runs it); the keystone
  read from `simulation/BASELINE.md`, never assumed. `sizeof(Node)` expected unchanged — if any struct moves,
  the deliberate-assert discipline + **the B246 probe re-pinned** (it is standing now — run it for any
  touched struct).
- Board evidence is now CERTIFIED: if flash/RAM moves, measure with `tools/measure_board.py` on the two
  approved envs and attribute (the B205/B255 method). ⚠ SCOPED board exception for exactly that measurement;
  compute only, no flashing.
- Native (RUN the binary, PIN derivation written) · touched-target batteries full pass · `git diff --check`
  clean · warnings unchanged.
- ⛔ NEVER `git commit`/`git add`/`git checkout --`. ⛔ No docs (register/frames/protocol updates drafted in
  the REPORT — `docs/frames.md`/`docs/protocol.md` corrections if the caps were misdocumented are
  supervisor-landed), no `tracker.md`, no `platformio.ini`, no parallel-session files. No pollers; never
  pipe the runner. Metal residue: DRAFT the one real-air check (a cap-boundary DM on real radios) if the
  matrix shows anything host-unreachable; else say none.

## Report
The boundary matrix (every variant × every length, the four observations each) · B20's and B21's causes,
SEPARATELY, with `file:line` · the two flipped reproductions · the one-length-authority reconciliation ·
the mutation ledger + full passes · the corpus-occurrence measurement + movement prediction · native + PIN
derivation · any board measurement with attribution · the DRAFTED doc corrections (frames/protocol if owed)
· exact final `git status --short`.
