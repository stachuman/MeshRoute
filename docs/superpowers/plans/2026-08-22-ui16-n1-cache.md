<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §UI-16 slice 2 — the nearby-team observation cache (N1) · dispatch brief · 2026-08-22

**Status: DISPATCHED. The APPROVED spec is the AUTHORITY:**
`docs/superpowers/specs/2026-08-22-ui16-nearby-onboarding-spec.md` — implement its **N1** slice exactly, with the
owner's OQ-2/OQ-5 rulings as folded (10 min = two default team-beacon periods · 8 entries · signal via the
EXISTING SNR EWMA, ⛔ never max-seen · the record ordered `uint64_t last_ms` FIRST targeting 16 B, SUBJECT TO the
required ABI measurement · an ORDERED BOUNDED ARRAY — refresh in place, shift out the oldest when full, append —
making first-observed order STRUCTURAL). ⛔⛔ **THIS IS THE ARC'S ONE `lib/core` SLICE** — the corpus obligations
below are not optional. ⛔ **NO DEVICE CONTACT** (the owner's nodes may be on the bench).

## Scope (N1 per the spec; its pins/mutations govern)
- The Node-global RAM ring, written at **ONE site** in `node_beacon.cpp` (the spec names where the foreign
  type-5 TLV is parsed-then-dropped today), two **`const` accessors** for the later UI projection. ⛔ READ-ONLY
  observation: no route, peer binding, membership, key or NV write; no TX; no timer id; **zero telemetry** (⛔ no
  `MR_EMIT` — the [[B169]] lesson: an emit-only local breaks every board build via `-Wunused-variable`).
- ⛔ ZERO wire bytes. ⛔ No UI this slice (N2 consumes the accessors later).
- The eligibility rule per the spec (a foreign non-zero team id observed on our current PHY). ⛔ **CORRECTED IN
  PLACE 2026-08-22 (QG N1-gate finding 3): this line read "own/current team EXCLUDED per the spec's rule" — that
  CONTRADICTED the approved spec (:568), which records the own team like any other and has N2 filter it at
  display. The implementation followed the SPEC and is correct; the brief was the defect.** The standing lesson:
  a brief paraphrasing a spec rule must quote it, not remember it.

## The corpus/D2 obligations (the ⛔ non-negotiables of a `lib/core` slice)
- **s18 inertness is an ARGUMENT you must make AND the md5 QG runs must confirm:** the cache writes RAM only, emits
  nothing, branches no existing behaviour — state precisely why no emitted event can move (the write site is
  after/beside a drop, never replacing one). If ANY existing branch must move to reach the TLV, STOP and report
  before touching it.
- **`sizeof(Node)` moves** — update the assert DELIBERATELY with the measured old→new figures and the per-field
  arithmetic; the per-board RAM diff is QG's (native alignment hides board padding — D2), but report the native
  delta and the expected board shape.
- Warnings: ⛔ **CORRECTED IN PLACE 2026-08-22 (QG N1-gate finding 4): for N1 the census requirement is REPLACED
  by a warning COMPARISON + `-Wswitch` zero on the TWO explicitly named environments** (`heltec_mobile` — Xtensa,
  full team; `gateway` — MR_FEAT_TEAM=0) — ⛔ never expanded to a third census env (the owner's standing two-env
  limit, which this brief's original wording collided with).
- **Read the CURRENT s18 keystone from `simulation/BASELINE.md` if you cite it — NEVER assume or hardcode it.**

## Operational contract (standing)
- C1: N1 only. Everything landed survives. The new pure decisions (eligibility, ring policy, EWMA fold-in) go
  where a gate can attack them — if the ring logic can be a pure header unit driven by the native suite (the
  spec says how), it gets its OWN battery target; the `node_beacon.cpp` residue is the one write site.
- Batteries: iterate; ONE full pass per touched target; sync `PIN_*` on moves; exit 6/7/8 = fix the invocation;
  never probe+battery concurrently; wait-loops on runner markers.
- Verification you run (QG runs the corpus and the boards; ⛔ **CORRECTED 2026-08-23 — this line read "corpus +
  boards + census"; for N1 the census is REPLACED by the two-env warning COMPARISON on `heltec_mobile` and
  `gateway` with `-Wswitch` = 0**, per the owner's two-env limit): native (RUN the binary) · touched-target
  batteries · `git diff --check` clean · the `lib/` diff EXACTLY the N1 scope.
- ⛔ NEVER `git commit` / `git add` / `git checkout --`. ⛔ Do not touch the register, the bench script, any
  plan/brief, either spec, the design doc, `tracker.md`, `docs/manual/`. Doc/bench text N1 forces: DRAFT in the
  report.

## Report
Every N1 pin with case name and match count · the measured record ABI (the 16-B claim confirmed or corrected) ·
the s18-inertness argument stated for QG · the `sizeof(Node)` old→new with arithmetic · the mutation ledger +
full-pass proofs · native counts + cross-check · the exact `git diff --stat -- lib/` · exact final
`git status --short`.
