<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# §B206/B138 slice S1 — one build-identity authority · dispatch brief · 2026-08-28

**Status: ✅ COMPLETE — QG-PASSED 2026-08-28 (9/9 controls RED; the measured extern seam accepted; B138 stays CLOSED, B206 CLOSED with S2 the same day). The APPROVED spec was the AUTHORITY:**
`docs/superpowers/specs/2026-08-27-b206-b138-deterministic-board-measurement-design.md` — §2 (S1's required
shape, behaviour contract, structural gate, as amended 2026-08-28) and §4-S1. Read §1 for the verified defect
(B138: two TUs embedding `__DATE__ " " __TIME__` straddle a second, literal merging is lost, `.text +16`).
⛔ **NO DEVICE CONTACT.** ⛔ S2 (the runner), B253 and B205 are separate slices — touch nothing of theirs
(⛔ no `tools/git_rev.py` change).

## Scope (spec §2, binding)
- `src/firmware_commands.cpp` becomes the ONE owner of the build timestamp and Git-revision strings;
  `firmware_commands.h` exposes read-only accessors (`build_stamp()` / `git_revision()` — or the
  `extern const char[]` shape; choose by the SMALLER MEASURED image, per the spec). `fw_main.cpp` no longer
  defines or reads `GIT_REV`/`__DATE__`/`__TIME__` — both the banner and the BLE `version` arm consume the
  authority.
- **Behaviour contract:** boot/USB banner text unchanged · BLE `version` JSON fields/spelling unchanged ·
  both transports report byte-identical strings in one image · normal builds still report the real compile
  time · the `nogit` fallback REMAINS (fail-loud is B253's).

## Structural gate (spec §2.3 — each item a check/control)
- production `src/` contains exactly ONE `__DATE__` and ONE `__TIME__` use;
- exactly ONE `GIT_REV` value definition/fallback;
- both output paths call the same authority;
- `lib/` contains zero `__DATE__`/`__TIME__`/`GIT_REV` uses (the amended pin);
- ★ the incremental-recompile pair, measured on `gateway` (the ABI that reproduced B138): recompiling ONLY
  `fw_main.cpp` cannot add a second timestamp string; recompiling ONLY `firmware_commands.cpp` still leaves
  exactly one.
Wire these into the existing build-identity probe / a focused check per the tree's harness idioms, with
controls that go RED when a second `__DATE__` is reintroduced in `fw_main.cpp` or the accessor is bypassed.

## Gate (spec §4-S1)
- native green (RUN the binary) — counts expected unchanged unless a test is added (derivation written if so);
- simulator rebuild: no relevant compile action; s18 exact (read the keystone from `simulation/BASELINE.md` —
  never assume it) — `fw_main.cpp`/`firmware_commands.cpp` are compiled by nothing the simulator builds:
  verify and state it;
- ⚠ **EXCEPTION to the standing no-board-builds rule, scoped to this slice's subject** (the slice IS the board
  build): you run the §2.3 incremental controls on `gateway` and ONE clean `heltec_mobile` build — compute
  only, ⛔ no flashing, no device. The formal two-env RAM/flash gate remains QG's. Expected: **zero RAM
  movement on both**; flash movement attributed to one fewer literal plus seam code, ⛔ not assumed zero —
  report the measured delta with the attribution.
- `git diff --check` clean; your diff stays `src/firmware_commands.{h,cpp}` + `src/fw_main.cpp` + the
  probe/test files + nothing else.

## Operational contract (standing)
- C1: S1 only. Batteries per the runner's header if any battery target file moves (check `TARGET_SRC` —
  `fw_main.cpp`/`firmware_commands.cpp` are likely non-targets; state the check). ⛔ never pipe the runner;
  ⛔ no background pollers; ⛔ never edit a battery target mid-run.
- ⛔ NEVER `git commit`/`git add`/`git checkout --`. ⛔ No docs (register/BASELINE/spec updates are landed on
  PASS by the supervisor), no `tracker.md`, no `platformio.ini`, no `tools/`ccache/git_rev files, no
  parallel-session files. Metal residue: none expected (a banner-text-unchanged proof on glass is covered by
  the byte-identical contract) — say so, or draft one line if you find a genuinely host-unreachable check.

## Report
The authority's landed shape (accessor vs extern, with the measured-image justification) · every structural
check with its control RED · the incremental-recompile proofs on gateway (the B138 mechanism killed) · the
banner/JSON unchanged proofs (byte-level) · the gateway/heltec_mobile build figures with the flash attribution
· native counts · the s18-inertness statement · exact final `git status --short`.
