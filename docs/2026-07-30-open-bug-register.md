<!-- Author: Stanislaw Kozicki <cgpsmapper@gmail.com> -->
# ACTIVE BUG REGISTER

Authoritative high-level work queue as of 2026-08-17. No identifier was removed, reused, or renumbered.

Detailed history lives in [the original evidence archive](archive/2026-08-12-bug-register-history.md) and the
[complete pre-cleanup snapshot](archive/2026-08-17-bug-register-pre-cleanup-snapshot.md). Search those files and the
named spec/evidence before implementation. Old line numbers are hints only; relocate by symbol. This file contains
dispatch state, not full investigation narratives.

Status rules: **OPEN/READY** permits scoped work; **METAL** requires the named hardware check; **OWNER CALL** requires
a decision; **PARKED/RECORD ONLY** is not **dispatchable** without a new ruling. Closed and superseded records live in
the archives.

## Recommended execution order

1. ★ **B209-B212 as ONE B207 follow-up package, in FOUR independently-gated micro-slices (QG-ruled 2026-08-18): (1) B209 retune-only core seam · (2) B211 preserve + report `sf_list` · (3) B210 truthful DAD reporting · (4) B212 command-specific validation/reporting.** Then bench **27.6** on a temporary static node, then B207 closure. ⓘ Original wording follows. **B209, then B207 closure** — fix the metal-confirmed team-PHY/home-registration coupling before treating the B207 transaction as final; then run bench Part 27. The B207 gate had passed before this cross-spec behavioural case was exercised. ⓘ The stack question remains **settled as a METAL QUALIFICATION, not a code blocker**: the 256-B `NodeConfig` copy was measured to **scalarise away** and was never the cost; growth is the typed carriers, ARM `handle_team` 536 → 856. Prerequisite for OLED UI-15.
2. **Metal in parallel** — ✅ **B196 Part 26 PASSED 2026-08-17 (CLOSED)**; still owed: B164 Part 22 TxDone/UI, B193 Part 20.5 NV power-cut, and ★ **B207 Part 27 on the newly committed provisioning image**.
3. **Gate reliability** — B206 and its relationship to B138, then the small B205 build fix.
4. **Message correctness** — B20/B21, then B35 and B159.
5. **Hybrid RTS closure** — B251 and B161 are closed; re-evaluate B157/B153 next. B112 remains a separate non-blocking issue, and B158 is a later independent jitter redesign.
6. **Mobile-home** — B151 scenarios, B178 canvassing, then B186b recovery.
7. **Product backlog** — B134 durable inbox, O4 BLE key-export security, then remaining UI/documentation debt.

Metal checks may run while independent implementation proceeds.

## Current blockers and near-term work

| ID   | Status                                                                                                   | Dispatch point                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B207 | ✅ **ALL QG BLOCKERS CLOSED 2026-08-17 (3 rounds) — gate green, awaiting final re-check. ⛔ UNCOMMITTED** | Typed save-before-apply transaction built per [the approved v4 design](superpowers/specs/2026-08-17-team-provisioning-transaction-design.md) — new `src/firmware_provisioning_service.h`; `handle_team` reduced to parse → typed request → render, and QA-gate **verified at the code** that it retains none of the six B207 mutations (they moved into the post-save `DeviceProvLive` adapter, `src/firmware_config.cpp:1085-1112`). `install_key` is **`void`**; `adopt`/`adopt_priv`/`mint` are structurally inexpressible on the seam. One blob authority kept (U2): `blob_take_team_channel_key` delegates to `blob_put_team_channel_key`. Notify gated on `verdict == applied` (`:1340`). ⛔ **CORRECTED: an earlier version of this row read as though QG had passed and the slice were green. QG HELD it** — see the defect list at the end. The AUTOMATED GATE is green and independently re-verified, which is a different and lesser claim. **Gate re-verified by the QA-gate, not relayed:** native **1680/83432 → 1704/83745 / 0 failed** (binary run; wrapper's false "0 test cases" reproduced), 14 native mutation controls RED, `probe_prov_tx` 6/6 + 6 controls RED, census unchanged at 174/178/178 with `-Wswitch` 0, four-step corpus inert (`b77cfd3d`, keystone `9868cad3`/269905 read from the anchor table, recompile control fired), `git diff -- lib/` **empty**, `sizeof(Node)` **221880** tripwire compiled. ⚠ **`tools/probe_board_ui` W12/W14-W20 were RE-PINNED** because the write now routes through `ICfgStore::save`; the new `CFG_ROUTED_SITES` term is **earned, not free** — QA-gate confirmed it is gated on the route being present (`run.sh:412`), so it cannot absorb a missing notify. ⛔⛔ **OWNER CALL: the team console path's stack peak grew ~224-312 B** (ARM `handle_team` frame 536→848 with the callees inlined; xtensa deepest chain 704→928), the largest single item being the **spec-mandated** 256-B `NodeConfig` role-projection copy (§3.3 forbids hand-deriving the role). **No metal `stackhw` reading exists** — the coder correctly touched no hardware while the [[B196]] soak runs. This path has recorded stack-overflow history. ★ **Six behaviour changes to expect:** `team 0` + any PHY arg now **refuses** (was a silent no-op) · a static node's `team new freq=…` now **honours** the PHY and reports bad tokens instead of ignoring them · a truly-unchanged same-team request reports **no change** with zero save/apply · two refusals swapped order (key error now reported before role) · the incomplete-PHY check reads the **candidate + build floor**, not live state · the private key no longer survives in a static scratch buffer. ⛔ **UNCOMMITTED (D4).** ⛔ Does **not** close [[B193]]: fake-store tests cannot establish physical NV atomicity. **Metal residue → bench Part 27.** ★★★ **PART 27 RUN 2026-08-18 ON `fc89e14`: SEVEN PASS, THREE NOT-RUN, FOUR NEW DEFECTS FOUND.** ✅ **27.8 (the round-3 live-divergence fix) PASSES on metal** — with the record and the request identical and only the radio diverged, the verb APPLIED (`↻ rx-freq → 869.4625` + `> team PHY:`), where the pre-fix behaviour would have discarded it as `no change`. ✅ **27.5 is the decisive one:** a same-team re-key applied while the node was keyless, preserved `team_local_id=220`, and **BOTH the id and the key survived a power-cycle** ⇒ the candidate did NOT persist `team_local_id = 0` on a same-membership request, which is precisely the defect design v2 corrected. ✅ Also 27.1 (stack: `stackhw` ≥ **4408 B free of an 8 KB task = 54 % headroom**, 4.4× the floor — ⓘ the 1504 B the verb consumed covers the WHOLE chain incl. LittleFS and X25519, ⛔ not attributable to this slice's 888 B frame), 27.2, 27.3, 27.4, 27.9. ⛔ **NOT RUN, each with a reason: 27.6** (needs a static node; both bench nodes are mobiles and `team 0` does not demote), **27.7** (no way to provoke a real NV write failure), **27.10** (the state is unreachable by construction). ⛔⛔ **The run found FOUR defects — [[B209]] [[B210]] [[B211]] [[B212]] — and exposed THREE of Part 27's own checks as unable to test what they claimed** (a field absent on the target platform, an expectation contradicting the check's own point, an unreachable premise); all corrected in place. ⇒ **the metal half of this row is DISCHARGED apart from the three not-run items; final closure waits on [[B209]] per the owner's execution order.** ⛔⛔ **QG HOLD 2026-08-17 (round 3) — ONE BEHAVIOURAL BLOCKER: `no_change` COMPARES ONLY AGAINST NV AND IGNORES LIVE DIVERGENCE, so an explicitly requested change can be SILENTLY DISCARDED.** `src/firmware_provisioning_service.h:526` is `!membership_changed && !differs`, where `differs` is candidate-vs-**record** only, and `:397` documents the live blind spot rather than closing it. **Concrete failure:** `mobile register freq=…` mutates the live config and radio **without** updating the record (`adopt_mobile_phy`, `lib/core/node.cpp:869`) ⇒ a later `team <current> freq=<the persisted value>` yields a candidate identical to NV ⇒ `no_change` ⇒ **the explicitly requested PHY is never applied and the radio stays on the divergent live PHY.** The same holds for an explicitly supplied key when the LIVE key is absent or different. ⚠ **The QA-gate had scoped this out as an owner choice; QG overrode that, correctly — an explicit command must not be discarded because NV already contains its requested value.** ⇒ **REQUIRED RULE: `no_change` = candidate equals the persisted record **AND** membership already matches **AND** every EXPLICITLY REQUESTED live domain already matches.** ★ **And it is cheap — the accessors already exist, verified by the QA-gate: `Node::active_freq_mhz()` (`lib/core/node.h:388`), `Node::active_bw_hz()` (`:375`), `cfg.layers[0].routing_sf` / `allowed_sf_bitmap` (`lib/core/node_carriers.h:33-34`), and `team_channel_key_present()` / `team_channel_pub()` / `team_channel_priv()` (`node.h:224-226`).** ⛔ The earlier claim that *"`NodeConfig` has no `freq_mhz`"* was true only of the top-level struct and is not a reason to defer. Controlled cases owed: **persisted PHY equals request but live PHY differs ⇒ `applied` with EXACTLY ONE PHY application**; **persisted key equals request but live key differs/absent ⇒ `applied` with EXACTLY ONE key installation.** ✅ **CLOSED — QA-gate verified at the code.** The rule is now `!membership_changed && !differs && live_phy_matches(...) && live_key_matches(...)` (`src/firmware_provisioning_service.h:604-606`), with `live_phy_matches` (`:325`, all four PHY fields) and `live_key_matches` (`:339`). `ProvSnapshot` gained four live-PHY scalars plus **POINTERS** to the live keypair (`:112-125`) — ★ pointers deliberately, so no second copy of the team secret enters the console frame, and `team_channel_pub()/priv()` already return `nullptr` for "no key" so the absent case needs no flag. Filled by the **adapter** at `src/firmware_config.cpp:1353-1358` from `active_freq_mhz()` / `active_bw_hz()` / `layers[0].{routing_sf,allowed_sf_bitmap}` / `team_channel_p*()` — ⛔ no new core accessor, no `NodeConfig` copy, no second `Blob`. ★★ **The service remains PURE — QA-gate measured it with comments stripped: `g_node` / `g_hal` / `g_persist_team_local_id` / `LORA_FREQ` / `LORA_BW` all ZERO** (the raw hits are prose); pinned by probe **S12** with control **C15** RED. ★ **A finding the coder added beyond the brief: the key comparison includes the PRIVATE half**, because `team_channel_key_load` installs verbatim — a record whose halves disagree would otherwise yield a node whose pub matches while its priv cannot decrypt. Control **M-E** (compare the public half only) is RED. **Gate re-verified by the QA-gate:** native **1710/83827 → 1713/83937 / 0 failed** (binary run), `probe_prov_tx` **12/12 + 16 controls RED** (⚠ **C10 went UNUSABLE at match count 0 when the phase-2 signature moved and was RE-PINNED — reported, not silently passed**, which is the standing rule for a moved mutation target), `probe_board_ui` unchanged, census 174/178/178 `-Wswitch` 0, corpus four-step inert with the recompile control firing, `git diff -- lib/` **empty**. **Six envs SUCCESS, RAM +0 EVERYWHERE**, flash +0/+0/+288/+252/+272/+228 — both gateway envs byte-identical because `handle_team` compiles out at `MR_N_LAYERS == 2`. ⚠ **Stack moved again and rides the ACCEPTED metal qualification, not a new design change: ARM `handle_team` 856 → 888 (+32, the extended snapshot); xtensa deepest chain 976 → 928 (−48).** ★ **New operator-visible behaviour: a `team <current>` naming a PHY or key the RADIO/NODE does not already hold is now APPLIED** (one coalesced save, one live application, still zero airtime) — previously discarded as `no change`. ⓘ **[[B206]]'s mechanism was measured here:** two `__DATE__` TUs landing in different seconds produce two un-deduplicated stamp strings, +16/+32 flash (`gateway` 481620 vs 481588; `xiao_sx1262` 533588 vs 533572) ⇒ **flash arms must be single-stamp on BOTH sides.** ⛔⛔ **QG HOLD 2026-08-17 — THREE IMPLEMENTATION DEFECTS, all verified at the code by the QA-gate:** ① ★★ **`no_change` is computed from the REQUEST SHAPE, not from the candidate.** `src/firmware_provisioning_service.h:405` reads `!membership_changed && key_action == preserve && !phy.present` ⇒ **(a)** re-supplying the ALREADY-ACTIVE PHY reports `applied`, runs live PHY handling and notifies the UI; **(b)** supplying the IDENTICAL existing key reports `applied` and reinstalls it; **(c)** ⛔ the dangerous one — a **role-projection-only** candidate correction (same team, no key, no PHY) is classified `no_change` and therefore **SILENTLY DISCARDED**. ⇒ derive no-change from the **candidate's actual differences against the stored record**, tracked while staging fields (⛔ not via a second `Blob` copy — see ③), with controls for identical-PHY, identical-key and role-only. ② ★★ **A NEWLY ASSIGNED `team_local_id` MAY NEVER REACH NV.** The candidate persists `team_local_id = 0` (`:325`) through `ICfgStore::save`, which ⛔ **does not update `g_persist_team_local_id`** — that tracker is written ONLY at boot restore (`src/fw_main.cpp:797`) and after a successful `persist_cfg_if_needed` save (`:1042`). So the tracker still holds the OLD team's id while NV holds 0; if DAD then happens to pick **the same numeric id the old team used**, `team_changed` at `src/fw_main.cpp:1030` is FALSE, nothing is persisted, **NV stays 0, and the next boot needlessly re-DADs** — the very outcome design v2 corrected. ⇒ after a successful save, **sync the tracker to the saved value (0) before DAD**, or force the post-DAD persist; ⚠ the sync belongs in the **adapter/caller**, not the pure service (which cannot see `fw_main`'s globals). Add a deterministic old-id-reuse test. ③ ⛔ **A COMMENT ASSERTS A PROPERTY THE CODE DOES NOT HAVE** — `src/firmware_provisioning_service.h:299-300` claims the 256-B `NodeConfig` projection copy *"dies at the closing brace, never alongside the Blob AND the plan AND a keypair"*, but `apply_team` (`:431`) holds the plan and the candidate live across that block, so **they do coexist**. ⇒ QG recommends **splitting role projection into an earlier phase whose copy is destroyed before the `TeamPlan` and `Blob` exist, then RE-MEASURING** the stack. ★★ **STACK — RESOLVED AS A MEASUREMENT, AND THE PREMISE WAS REFUTED (2026-08-17). ⛔ WITHDRAWN IN PLACE:** this read *"The stack increase is NOT approved as-is (ARM 536→848 B, xtensa 704→928 B on a path where `stackhw` once fell to 72 B)"*. **The 256-B `NodeConfig` copy is NOT the cost and never was:** ARM `handle_team` measures **856 B with the projection first, 856 B with the plan+Blob first, and 856 B with the copy DELETED OUTRIGHT** — `-Ofast` scalarises it, because `role_enforce` touches exactly three fields (`lib/core/node_role.h:79-85`). ⇒ **the growth is the TYPED CARRIERS** (`TeamRequest` 136 + `TeamPlan` 128 + `ProvResult` 64 vs the old frame's 64 B of `tk_pub`/`tk_priv`); pre-slice ARM was **536**, and the QG-requested split bought **+8 B** (`sizeof(TeamProjection)`), not a reduction. ⓘ xtensa deepest chain is **976** (304 → 592 → `apply_phy` 80); the earlier **928** took a shorter leg and understated the peak by 48 B. ⛔ **The "72 B" history is WITHDRAWN as an explanation of present risk** — it predates the dedicated 8 KB mesh-task fix that moved this work off the 4 KB Arduino loop stack, so it does not describe current headroom. ⇒ ★ **ACCEPTED AS A METAL QUALIFICATION rather than a code blocker (QG 2026-08-17): bench Part 27 must be run on the resulting firmware IMMEDIATELY.** Reducing the frame would mean changing the seam's carriers (drop `TeamPlan::phy` ~40 B, let the plan reference the request's key buffers ~64 B) — a design change, ⛔ not authorised. ④ **Output overclaim:** `src/firmware_config.cpp:1184` says *"no flash was changed"* on the failure arm — ⛔ **not established**, since a backend may fail after a partial physical write ([[B193]]). ⇒ say only **"persistence failed; no live state was applied and no airtime was spent"**. |
| B209 | ✅ **FIXED + QG PASSED 2026-08-18 — ⛔ UNCOMMITTED, metal-verify pending (bench 27.11)** | **`team new` / `team <id>` with `freq=… sf=… bw=…` silently authorises static-home attachment even when `mobile_autoregister=false`.** Metal reproduction: after `team new freq=869.4625 sf=7 bw=125`, `mobile status` reports the contradictory but diagnostic tuple `autoregister:false`, `home_desired:true`, `attachment:"seeking"`, followed by repeated outbound mobile J `DISCOVER`s. Root cause is exact: `DeviceProvLive::apply_phy()` (`src/firmware_config.cpp:1099-1108`) calls `Node::mobile_register_phy()`, whose documented contract (`lib/core/node.h:647-649`) is **retune + `mobile_request_home_service()` + immediate DISCOVER**. The durable retries are therefore expected after the wrong authorisation is created; this is not a retry-timer defect. **Cross-spec conflict:** the B207 transaction spec records the old call as “retunes and kicks the FSM” (§1 line 40), but its normative seam says `apply_phy` is the PHY operation and `fire_dad` is the airtime operation (§3.1-3.2). The mobile-home spec is explicit: auto-OFF stays dormant until explicit `mobile register`, Heltec has no hidden registration, and required gate 9 says auto-OFF emits nothing; `test_node_join.cpp` already pins an auto-OFF team member performing team-DAD with zero DISCOVERs. **Required correction:** make provisioning `apply_phy` use a clearly named retune-only core seam built on the existing `adopt_mobile_phy()` mechanism; it must preserve `home_desired`, attachment state and registration timers. Keep `mobile_register_phy()` unchanged for the explicit console/app registration verb. `fire_dad()` remains the separate, last team-plane airtime operation. Correct the provisioning spec's inherited wording in the same slice. Pin: (1) auto-OFF + team create/join with PHY ⇒ team-DAD/local id succeeds, `home_desired=false`, `dormant`, zero mobile DISCOVER; (2) explicit `mobile register freq=…` still enters `seeking` and DISCOVERs; (3) an already-authorised manual/auto session remains authorised across a team PHY apply without a second hidden authorisation; (4) mutation routing the adapter back through `mobile_register_phy()` turns the regression RED. ⓘ Metal workaround: `mobile unregister` stops the loop without leaving the team or clearing the team-local identity. ★★★ **FIX LANDED 2026-08-18.** ⓘ **ATTRIBUTION, CORRECTED — this row first read "verified at the code by the QA-gate" in a way that could be taken for a QG pass, and it was written BEFORE any QG review existed.** The honest split: **(a)** the QA-gate re-verified the code shape and re-ran native + `probe_prov_tx` on 2026-08-18 **before** review — that is a coder-claim check, not an approval; **(b)** ★ **an independent QG review PASSED the slice later the same day** with no implementation blocker, confirming the separation (provisioning retunes without authorising; the explicit verb keeps its behaviour; an existing authorised session is preserved without an extra discovery attempt) and requiring only the two documentation corrections applied here. New public retune-only seam **`Node::mobile_retune_phy`** (`lib/core/node.h:663`, inside the `#if MR_FEAT_MOBILE` block at `:635`, delegating to `adopt_mobile_phy` **and nothing else**; ⛔ no non-mobile `#else` stub invented — its neighbours have none). `DeviceProvLive::apply_phy` (`src/firmware_config.cpp:1114`) points at it; **`mobile_register_phy` (`node.h:648`) is PROVABLY untouched — `git diff -- lib/core/node.h` is +14 insertions and ZERO deletions** — and `handle_mobile` (`:1435`) still calls it, so the explicit verb keeps authorising. `fire_dad` untouched. ⓘ The drifted seam comment at `src/firmware_provisioning_service.h:284` — the wording that let the defect in — was corrected. ★★ **PIN 3's VACUITY WAS CONFIRMED BY MEASUREMENT, NOT ARGUED: under the seam mutation `home_desired` and `attach_state` BOTH PASSED; only `count_armed(kMobileDiscoverTimerId)` discriminated.** ⇒ QG's insistence on a timer-arm count was load-bearing — without it pin 3 would have been the eleventh instrument in this arc that could not fail. **Controls:** the mutation reddens pins 1 and 3 (5 assertions) while **pin 2 — the unchanged positive control — stays GREEN**, exactly as the corrected control rule requires; `C16`/`C17` redden probe `S13` (C17 covers deleting the retune outright, so banning the wrong verb cannot pass by removing the right one). **Gate re-verified by the QA-gate:** native **1713/83937 → 1716/83966 / 0 failed** (binary run), `probe_prov_tx` **13/13 + 18 controls RED**, census 174/178/178 `-Wswitch` 0, `sizeof(Node)` **221880**, `kCap` 91, `git diff -- lib/` = `node.h` only. ★ **Corpus — a STRONGER result than ordinary inertness:** the header edit forced a genuine **34-object rebuild** and `lus` still reproduced **`b77cfd3d`** byte-for-byte (an uncalled inline emits no code), with s18 `9868cad3`/269905 behind it; anchor table untouched. **Six envs: RAM +0 everywhere**, flash 0/0/−128/−20/−8/−16 — the four `MR_N_LAYERS<2` envs SHRINK, the expected sign now that `apply_phy` no longer inlines the authorisation and the timer arm. ⇒ **metal residue is bench 27.11 only.** |
| B196 | ✅ **CLOSED 2026-08-17 ON METAL — the fix is PROVEN and the mechanism is confirmed from both directions** | ★★★ **Part 26 PASSED with margin.** Headless soak on `cb76d79` (archive `~/MeshRoute-artifacts/soak-20260817-1002/`, ELF sha256 `3071ef553026e75a…`, identity proven by the embedded banner string): `uptime_ms=36805168` (10h13m) · **`slept=36932`** · `wkarmfail=0 wkbusy=0 wkdisarm=0 wksleepfail=2` ⇒ ★ **attempts = 36 934, which is 4 165 PAST the 32 769 trip point**, with **no panic and no reset** (`faults` shows the last `HARDFAULT` as boot **101**, `ran 9h02m29s` — the ORIGINAL defect on `a1e53dd`; boots 102-106 are deliberate power cycles). ⓘ `sleep=off-host` is an **artefact of the read** (typing `faults` latched `g_host_present`) and ⛔ is NOT a failed arm — `slept` is cumulative for the boot, so the ten hours of sleeping are recorded in it. ★★ **WAKE-CAUSE SELF-CONSISTENCY, the check that also validated §B200: `wk_gpio 1 + wk_ext1 120 + wk_tmr 36811 = 36 932 = slept EXACTLY`** — every sleep accounted for by exactly one cause, so the counters are not garbage. ★★★ **AND THE MECHANISM IS CONFIRMED IN REVERSE, which is the strongest evidence here: the FIXED build's measured attempt rate is 1.0035/s, at which the 32 769-call trip point falls at 32 655 s = 9h04m — and the observed panics were 32 318-32 635 s (8h58m-9h04m).** The prediction lands INSIDE the observed window, so the fixed image's own telemetry retro-predicts the broken image's failure time. ⓘ Independent liveness with **no console byte**: the second mobile (node 30) logged `rx BCN from=222` throughout at snr ~12 / rssi −69 — the first time this arc had third-party confirmation that cost nothing to obtain. ⛔ **Does not close [[B193]]** (physical NV atomicity) and does not bear on [[B199]]. Fix: one `esp_sleep_pd_config(ESP_PD_DOMAIN_RTC_PERIPH, ESP_PD_OPTION_ON)` per boot at `src/fw_main.cpp:924`, committed in `0173755`. |
| B164 | IMPLEMENTED / METAL                                                                                      | Bench Part 22 must prove physical TxDone drives `send_aired` and earned OLED wording; no more design is owed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B193 | ✅ **CLOSED 2026-08-22 (owner-confirmed) / METAL-QUALIFIED** | ~~Bench Part 20.5 must qualify reset-during-write; fake-store tests cannot establish physical NV atomicity.~~ ✅ **Part 20.5 (the `/mrcfg` reset-during-write test) RAN AND PASSED on the bench** — run-sheet §9 `[x]`, and the owner confirmed explicitly 2026-08-22 that the mark means ran-and-passed (the row had stayed open because the `[x]` alone was ambiguous against the sheet's "or mark not-run" idiom). The `/mrjoin` sibling was separately qualified by the §UI-15 slice-7 power-cut (walkthrough Phase 7, 2026-08-20). ⇒ both NV records are metal-qualified for reset-during-write.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| B206 | OPEN / GATE DEFECT                                                                                       | Make flash measurement deterministic; meanwhile rebuild both timestamp TUs or clean-build and verify one build-stamp string. ★★★ **TWO FURTHER CONFOUNDS REPORTED 2026-08-18 — ⛔⛔ CODER MEASUREMENTS FROM THE §B209 SLICE, *NOT* B209-QG FINDINGS AND *NOT* INDEPENDENTLY REPRODUCED.** They fall **outside B209's scope** and were not exercised by that slice's gate or by its QG review; the QA-gate did **not** re-run either measurement. ⇒ **treat both as leads to confirm under this row's own work, not as established facts.** ⓘ If they hold, a "B206-clean" measurement is still not reliable: ① ⛔⛔ **BUILD PATH LENGTH CHANGES THE IMAGE SIZE.** A BEFORE arm built in a `git worktree` under `/tmp` read `gateway` **481668** against **481588** for identical sources built in place — an **+80 B pure path artifact** on 5 of 6 envs, which would have been reported as a real delta. ⇒ ★ **BOTH ARMS MUST BE BUILT IN THE SAME DIRECTORY**, and ⛔ a `/tmp` worktree — the very isolation this project recommends for safe comparisons — is **INVALID for flash measurement**. ② **±16 B of build NONDETERMINISM survives the timestamp fix:** two clean builds of identical sources gave `heltec_mobile` 1280684 and 1280700, **each image holding exactly one build-stamp string** ⇒ the `__DATE__` merge loss is **not the whole mechanism**, and a single-stamp check does not certify a reproducible build. ⇒ **treat any flash delta under ~32 B as noise unless reproduced across two clean in-place builds per arm**; RAM is the trustworthy figure (+0 on all six envs in that slice while flash moved on four). |
| B138 | OPEN / RECONCILE WITH B206                                                                               | Reassess the older flash-reproducibility finding after B206; separate mechanisms before any second fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B205 | OPEN / SMALL BUILD FIX                                                                                   | Make `MR_NO_POWERSAVE` compile on ESP32 by matching the OTA include guard to its consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B204 | OPEN / LOW / METAL                                                                                       | Remove or demote the harmless boot `Incorrect wakeup source (7)` line without weakening the B200 scrub.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| B199 | OPEN / FIELD WATCH                                                                                       | Preserve exact ELF/log evidence and investigate only if the distinct watchdog-reset class recurs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## Protocol, routing, and message correctness

| ID | Status | Dispatch point |
|---|---|---|
| B20 | OPEN / HIGH | Encrypted DM lengths 215–216 can disappear without `send_failed`. |
| B21 | OPEN / HIGH | Oversized DM reports the wrong condition and no `send_failed`; investigate with B20 but retain distinct causes. |
| B24 | OPEN / TELEMETRY | `q_tx.rt_total` remains plane-inconsistent. |
| B25 | OPEN / UNMEASURED | A team member may answer a static sync with a team-plane beacon. |
| B31 | OPEN / POLICY | `key_hash_for_id` lacks authoritative-confidence and TTL gates. |
| B34 | OPEN / SIMULATOR | Simulator collapses refusal reasons to generic error. |
| B35 | OPEN / SILENT | Channel self-skip compares identifiers across planes. |
| B36 | OPEN / CONTRACT | Received-message JSON does not expose attached location. |
| B52 | OPEN / CONTRACT | JSON exposes team confidence but not static confidence. |
| B54 | OWNER CALL | Decide eviction policy when a first claim arrives in a full first-hand team table. |
| B55 | OPEN / CONTRACT | Document the S4b meaning of `reqpubkey_sent.hash == 0`. |
| B56 | OWNER CALL / CONTRACT | Decide how a stage-2 `reqpubkey` failure becomes app-visible. |
| B60 | OPEN / SPEC READY | Fix multi-gateway `send_layer` final-hash resolution after settling ACK policy. |
| B111 | OPEN | A queued DM with `ctr == 0` has no correlatable outcome kind. |
| B112 | OPEN / CORE / DOES NOT BLOCK B251 | A non-zero DM counter does not itself prove enqueue success: a `MOBILE_SEND` wrapper's first hop can still be ACKed before failed home re-origination. B251 prevents that failure from manufacturing correlation state or origin evidence; the broader first-hop-ACK contract remains separate. |
| B153 | OPEN / READY FOR RE-EVALUATION | Hybrid RTS implementation is substantially complete and the combined B251/B161 dependency is cleared. Reassess the cumulative closure conditions explicitly rather than closing this row by implication. |
| B157 | IMPLEMENTATION COMPLETE / READY FOR RE-EVALUATION | Exact implicit-forward comparison exists, B161 is closed and the QG-passed B251 boundary restores distinct hosted-mobile/home flights. Re-evaluate the universal safety disposition explicitly rather than closing this row by implication. |
| B158 | OPEN / REDESIGN | Design current MeshRoute-native retry jitter; do not copy obsolete Lua constants. |
| B159 | OPEN / CORRECTNESS | DATA de-duplication can expire inside the retry horizon and deliver a retry twice. |
| B160-COV | OPEN / COVERAGE | Add a scenario combining non-AUTO plane with requeue. |
| B160-SIB | OPEN / NARROW METAL PATH | `txitem_from_pending` drops channel/flood carrier state on one requeue path. |
| B161 | ✅ CLOSED / QG PASS / UNCOMMITTED | Canonical origin wrapping for DATA types 1/2/8/13 remains mutation-covered. The QG-passed B251 correction restores the required full corpus to 36/36 green, including s22 at 8/8 deliveries and zero assertions. Final historical/status bookkeeping is complete; B157 and B153 are ready for explicit re-evaluation. |
| B251 | ✅ **CLOSED / QG PASS / UNCOMMITTED — found by B161 full-corpus gate 2026-08-27** | The home is now a counter boundary for qualifying hosted-mobile plaintext static transit: first hop and hop ACK retain `ctrM`; direct by-id transit admits its outward queue and correlation before that ACK; the outward RTS/DATA use one home `ctrH`; and the strengthened bounded correlation ring translates the returned E2E ACK to `ctrM` without live eviction. A `MOBILE_SEND` hash wrapper retains the separate open B112 first-hop-ACK behavior, but `SendDispatch` is authoritative after PostAck: queued emits/activates, parked retains its reservation until admission, and refused releases it with no phantom origin evidence or mapping. First-hop loop de-duplication uses the verified mobile hash, while the completed cache uses the immediate sender, so two mobiles with equal `ctrM` and destination remain distinct. Full pressure returns BUSY_RX and `ctrrefuse=` before ACK; the existing bounded retry/give-up path remains authoritative. Team, ordinary static and CRYPTED traffic are unchanged. Final gate: native 2240/96469/0, 13/13 mutations RED, analyzer 194/194, corpus 36/36 with s22 8/8 and s18 exact; independent QG PASS. No frame-length change. B112 remains separately open and does not block this closure. |
| B165 | OPEN / MEASUREMENT | Determine whether the RTS duty-defer slot can overwrite a live deferred RTS. |
| B166 | OPEN | NAV prices a 3-byte CTS although a legal 4-byte CTS can fly, releasing early. |
| B167 | DISPOSITION REVIEW | Restate the surviving timing/size residue before authorising code; byte-table half is closed. |
| B168 | OWNER CALL | Decide whether a ten-board end-of-arc warning sweep is a standing requirement. |

## Mobile-home and simulation

| ID | Status | Dispatch point |
|---|---|---|
| B140 | OWNER CALL | Resolve the remaining re-anchor/byte-identity question against the post-B182 metric. |
| B141 | OPEN | Repair timing-fragile s28 separately from the possible channel-overhear guard defect. |
| B151 — missing §12.2 integration scenarios | READY / PLAN | Add late-home and auto-OFF cases from `2026-08-11-b151-integration-scenarios.md`. |
| B178 | OPEN / NARROWED | Measure candidate-aware proactive canvassing; trigger-2-only remains interim policy. |
| B181 | DISPOSITION REVIEW | Retain only scenario work surviving B182; do not implement the metric-corrupted premise. |
| B184 | OPEN / SIMULATOR | Wrapper cannot express hash-addressed DM with E2E ACK although firmware can. |
| B186 | B186a DONE / B186b OPEN | Observability is done; design recovery for asynchronously refused mobile J frames, including B189 stale-stash residue. |

## Heltec/OLED and product backlog

| ID | Status | Dispatch point |
|---|---|---|
| B83 | OPEN / CALLER CONTRACT | Confirm every UI lifetime closes the send tracker's bounded `late_ack` slot. |
| B92 | OPEN / DOC-ONLY | Reconcile obsolete spec font names with implemented fonts. |
| B93 | OPEN / LATENT | Replace hard-coded `meshroute` forward declaration in `mr_ui.h` with configured namespace. |
| B104 | OPEN / COVERAGE RESIDUE | Extend feature-probe coverage beyond call counts where practical; geometry remains metal. |
| B119 | OPEN / SOURCE DOC | Correct `Push::enc` documentation; encrypted channel receive already sets it. |
| B124 | OPEN / DEDUP | Centralise duplicated 1S-LiPo plausibility window. |
| B125 | OWNER CALL | Decide whether unavailable battery reads retain the last good value indefinitely. |
| B131 | OPEN / DOC-ONLY | Remove or replace nonexistent `MR_UI_BLANK_MS` reference. |
| B134 | OPEN / PRODUCT | Provide durable ESP32/Heltec inbox storage; deleted records can return after reboot. |
| B208 | OPEN / SMALL / CONSOLE UX | **Section the oversized console help instead of increasing RAM.** Reproduced on metal: bare `help` attempts the current ~6121-B / 75-line monolithic response against the fixed 2048-B guarded-console stage, so only the first sections arrive and the operator gets `!! CONSOLE_DROP lines=43`. This is the explicitly documented `src/console_sink.h` capacity outcome, not client-side truncation. **Owner ruling 2026-08-17:** bare `help` and `?` print only a compact index of the help topics available in that build; `help <topic>` prints exactly one complete section. Canonical topics follow the existing headings: `messaging`, `identity`, `mobile`, `inbox`, `diagnostics`, `remote`, `test`, `provisioning`, and `cfg`; feature-gated topics are listed/accepted only when compiled. An unknown topic must print usage plus the valid topic names. Each individual response must fit the existing `MR_CONSOLE_STAGE_BYTES` and produce no `CONSOLE_DROP`. **Do not** enlarge the 2-KB stage, add a blocking/asynchronous pager, or bypass `GuardedConsole`. Pin: compact-index completeness, every topic's complete output and size bound, conditional-topic behaviour, unknown-topic refusal, `?` parity, and structural coverage proving no existing help line/command disappears during the split. |
| B210 | ✅ **FIXED 2026-08-18 — ⛔ UNCOMMITTED, metal 27.14 pending** | **The `  team-DAD: local_id=N` line is printed on EVERY applied team command, whether or not team-DAD actually ran — it reads as an airtime claim and is only a status echo.** `src/firmware_config.cpp:1403` gates it on `res.team_id != 0 && g_node.config().is_mobile` and ⛔ **never consults the verdict's `fire_dad`**, which is the real airtime decision (`src/firmware_provisioning_service.h:235`, set only when `membership_changed && team_id != 0 && projected_is_mobile`). **Metal reproduction 2026-08-18 (Part 27.8):** a same-team PHY re-apply on the Heltec printed `team-DAD: local_id=32` on **both** invocations although membership never changed and no DAD fired — corroborated by `team_local_id` staying **32** throughout and by the ABSENCE of the `»tx BCN from=32` burst that a real DAD produces (the XIAO's genuine `team new` DAD did emit `»tx BCN from=155 n=0` immediately). ⚠ **It actively misled a bench check:** Part 27.8 originally used the line's absence as the discriminator for "no DAD, no airtime", which cannot work. ★ **This is the MIRROR of the project's standing rule that a display-shaped field must never make an airtime decision** — here a display field ASSERTS an airtime event that may not have occurred. **Required correction:** gate the line on the transaction's actual `fire_dad` (already known and returned), or relabel it so it cannot be read as a claim that DAD ran. Pin: a same-team re-apply must not print a line implying DAD; a genuine membership change must. ⓘ Part 27.8's discriminators were corrected in place to `team_local_id` unchanged + no beacon burst. ✅ **FIXED 2026-08-18 (slice 3). ⛔ UNCOMMITTED.** `src/firmware_config.cpp:1451` now reads **`if (res.dad_fired)`**, replacing `res.team_id != 0 && g_node.config().is_mobile` — a condition true of *every* applied team command on a mobile. ★ **The truth already existed and was simply ignored:** `ProvResult::dad_fired` is set at exactly one site (`src/firmware_provisioning_service.h:716`, inside `if (plan.fire_dad)`), and `fire_dad = membership_changed && team_id != 0 && projected_is_mobile` (`:619`) **strictly implies** both halves of the old gate — so they were dropped rather than kept, since re-adding them would hide where the decision is made. ⛔ The **relabelling** option this row recorded was **NOT taken**, and the two were not blended. ★★ **THE TRAP WAS AVOIDED AND IS PINNED: the id is still read LIVE (`g_node.team_local_id()`).** `res.persisted_team_local_id` carries the CANDIDATE's value, which is **`0` on precisely the membership-change case that now reaches this line** (design v2 — 0 means DAD-pending), so swapping to it would print `local_id=0` on every genuine DAD — a silent defect no build and no native case could catch. ⓘ **ACCEPTED CONSEQUENCE:** a same-team apply no longer shows the id on this line; it stays a `cfg`/`status` fact, and a line labelled `team-DAD` must mean DAD. ⛔ No substitute line was added. **Coverage is STRUCTURAL** (this TU is compiled by neither the native suite nor the simulator): `probe_prov_tx` **S17**, five clauses — gate present exactly once · old gate absent · live id read inside the gated statement · candidate field absent from it · **site after the `verdict != applied` guard, confirmed not assumed** — plus ★ **the counted discriminator: the literal occurs EXACTLY ONCE in raw source**, which is what makes deleting the line a FAILURE rather than a pass. Controls **C25-C29** all RED (old gate restored · gate deleted · gated on `team_id != 0` alone · id swapped to the candidate · whole line deleted); positive control **S16** stayed GREEN. **Gate:** native **1720/83996/0** unchanged (this TU is `test_build_src = no`), `probe_prov_tx` **17/17 + 30 controls RED**, census 174/178/178 `-Wswitch` 0, corpus four-step inert with the recompile control firing, `sizeof(Node)` 221880, two envs RAM **+0** both. ★★ **AND IT REPAIRS A BENCH INSTRUMENT: Part 27.8's discriminator — the ABSENCE of the `team-DAD` line on a same-team re-apply — is VALID AGAIN**, having been withdrawn when this defect made it meaningless. **Metal residue → bench 27.14.** |
| B211 | ✅ **FIXED + QG-PASSED 2026-08-18 — ⛔ UNCOMMITTED, metal 27.12/27.13 pending** | **A PHY tail's `sf=` SILENTLY REPLACES the DATA `sf_list`, which the operator never supplied and which is never reported.** `src/firmware_config.cpp:879-881` builds a fresh `LayerConfig{}` and sets `phy.allowed_sf_bitmap = (uint16_t)(1u << pa.sf)` — so `sf=` (the ROUTING/control SF) also collapses the DATA SF set to that single value, with **no comment justifying it**. **Metal reproduction 2026-08-18:** a Heltec booted with `data sf = 6,7` (its own banner: *"receiver picks the fastest by SNR"*); after `mobile register freq=868.5 sf=9 bw=250`, `cfg` read `routing_sf=9 sf_list=9`, and after `team … sf=7` it read `sf_list=7`. **SF6 was silently dropped from the DATA set and the change is PERSISTED** (the bitmap rides the candidate into NV), so it survives reboot. ⛔⛔ **QG-CONFIRMED A DEFECT 2026-08-18, AND THE RATIONALE THIS ROW ORIGINALLY OFFERED IS WITHDRAWN AS REFUTED BY THE PROJECT'S OWN POLICY.** It read: *"There is a defensible rationale — a team is a SHARED-PHY overlay and members must agree on `sf_list` as well as freq/sf/bw, so forcing one known value makes them match ⇒ this may be intended behaviour."* ★★ **`lib/core/node.h:302-305` defines team-PHY compatibility EXPLICITLY EXCLUDING sf_list:** a team member refuses a home whose PHY differs in *"freq/bw/routing_sf/cr; NOT layer_id … **NOT sf_list — F-SF-1 keeps that across registration**"*. ⇒ **team coherence does NOT require a common `sf_list`, so collapsing it buys nothing and costs DATA-SF diversity.** ⓘ The older team design likewise records that creation inherits `sf_list` unchanged. ⛔ **But intended or not, it is BOTH UNREQUESTED AND UNREPORTED: the `> team PHY: freq=… sf=… bw=… kHz` confirmation prints THREE fields while silently altering a FOURTH it never mentions**, which is the C2 (fail-loud / no silent unagreed change) objection independent of the routing question. ⓘ It also sits awkwardly beside the project's own `data_sf`-removal ruling, under which `sf_list` is a mandatory, explicit, fail-loud set rather than something derived from another field. ★ **QG RULING PROPOSAL (2026-08-18), recommended: (a) a team PHY tail PRESERVES the existing `allowed_sf_bitmap`; (b) ⛔ do NOT globally change `mobile register` semantics; (c) ALWAYS report the resulting `sf_list` in the confirmation line.** ⇒ awaiting the owner's ruling on (a)-(c); (c) is required whichever way (a) goes. ✅ **FIXED 2026-08-18 (slice 2), QG-passed. ⛔ UNCOMMITTED.** Owner-ruled shape implemented: the team path sends `rq.phy.allowed_sf_bitmap = 0` = *not specified* (`src/firmware_config.cpp:1358`) and `stage_team_candidate` resolves it **from the PERSISTED RECORD** at `src/firmware_provisioning_service.h:531` — ★ **verified to precede BOTH comparisons** (`differs` `:536`, `live_phy_matches` `:635`), which is what stops a same-PHY re-apply looking like a change and regressing metal-passed bench 27.8/27.9. ⛔ **The shared `parse_phy_tail` is UNTOUCHED** (it serves `handle_mobile` too). The echo now prints the resolved set through the **existing** `mrfw::print_sf_list(out, …)` — ⛔ no third formatter, sink passed not global: `> team PHY: freq=869.463 sf=7 bw=125.00 kHz sf_list=6,7`. ★★ **PIN 1b EARNED ITS PLACE: under the mutation resolving from `snap.live_allowed_sf_bitmap`, the verdict, the write count and `phy_calls` were IDENTICAL in both arms — only the preserved-field COUNT (3 → 0) discriminated.** Without QG's insistence on the stored-vs-live divergence case, pin 1 would have passed a coder reading live state. ⚠ **One further operator-visible change, stated not papered over:** a team PHY tail over a record whose `sf_list` is genuinely EMPTY now **REFUSES** (`incomplete_phy`) instead of silently inventing `sf_list={sf}` — C2-correct, but a node that somehow persisted an empty set is now stuck until it is set explicitly. ⛔ **A production comment was ALSO corrected (QG blocker):** `firmware_provisioning_service.h` claimed team members need a COMMON `sf_list` — the very belief the defect rested on; it now records that `sf_list` is **node-local**, preserved from the record, and checked here only for **non-emptiness**. **Gate:** native 1716/83966 → **1720/83996 / 0**, `probe_prov_tx` **16/16 + 25 controls RED**, census 174/178/178 `-Wswitch` 0, corpus four-step inert with the recompile control firing, `sizeof(Node)` **221880** via a compile-only probe **whose own control fired**, two envs (`xiao_sx1262`, `xiao_esp32s3`) RAM **+0** both, flash +16/+24 (under the ~32 B noise floor). **Metal residue → bench 27.12 / 27.13.** |
| B212 | ✅ **FIXED 2026-08-18 — ⛔ UNCOMMITTED, metal 27.2-diagnostics pending** | **Every `team` subcommand reports PHY range errors as `> team new err:`, and the generic range check MASKS the specific `team 0` refusal.** `src/firmware_config.cpp:1325` hardcodes `F("> team new err: freq 100..1000 MHz, sf 5..12, bw 7..500 kHz")` into the shared `PhyTailMsgs` used by **all three** forms (`team new`, `team <id>`, `team 0`). **Metal reproduction 2026-08-18 (bench Part 27.2):** `team 0 freq=868` answered `> team new err: freq 100..1000 MHz, sf 5..12, bw 7..500 kHz` — naming a subcommand the operator did not type. ★★ **The second half is the more serious one: the range check runs INSIDE the parser and pre-empts the specific refusal.** `phy_args_in_range` (`src/firmware_config_parse.h:106`) requires freq **and** bw **and** sf to be valid, so a partial tail (`freq=` alone) fails at `src/firmware_config.cpp:878` **before** the [[B207]] transaction can answer with the accurate *"freq=/sf=/bw= make no sense on `team 0` (leave) — leaving a team PRESERVES the current PHY"*. ⇒ the operator is told the values are out of range when the real answer is that the arguments are **meaningless on that verb**. ⓘ **The OUTCOME is correct in both cases — refused, nothing applied, team and PHY untouched (verified on metal via `cfg`) — so this is a DIAGNOSABILITY defect, not a behavioural one.** ⚠ **It has already cost something concrete: bench 27.2 was written as `team 0 freq=868` and therefore never exercised the branch it claimed to test**; the check was corrected in place to use a complete tail. **Required correction:** (a) label the message by the subcommand actually invoked, or make it verb-neutral (`> team err: …`); and (b) evaluate the `team 0` + PHY refusal on **any PHY token being present**, rather than only after a successful full parse, so the specific message wins over the generic one. ⓘ C2 is satisfied in letter (it fails loud) but not in spirit — loud with the wrong reason. ✅ **CLOSED 2026-08-18 (slice 4/4 of the [[B207]] follow-up). ⛔ UNCOMMITTED.** Both halves fixed in `src/` only — ★ **the service was already correct and was NOT touched** (`phy_on_leave` sits second in `validate`, `src/firmware_provisioning_service.h:393`, ahead of every other check), so there is no new `ProvErr` and no new message. ⛔ **MECHANISM CORRECTED 2026-08-18 (QG): `bw=` is OPTIONAL, defaulted to 125 kHz (`src/firmware_config.cpp:881`) — this row's earlier "requires freq AND bw AND sf" was FALSE, and the source comment at that line had said so all along.** ★ Two paths, not one: `freq=868` trips the range check because **`sf` stays 0**; `sf=7` or `bw=125` alone dies EARLIER at `!pa.has_freq` (`:889`). Both mask the leave refusal. **(a)** A pure, natively-tested classifier **`mrfw::classify_phy_tail`** (`src/firmware_config_parse.h:151`, enum `:146`) reuses `kv_next`/`phy_arg_take` — ⛔ no hand-written key list — and runs at `src/firmware_config.cpp:1362`, **ahead of `parse_phy_tail` (`:1372`)**, on the **leave form only**; the `phy_only` arm sets `rq.phy.present` so the transaction speaks. ★★ **Classification is by KEY RECOGNITION, never value validity:** `team 0 freq=99999` is prohibited PHY, ⛔ not a range complaint. Mixed tails (`team 0 freq=868 wibble=3`) **retain the existing unknown-key error** — the more actionable complaint is not swallowed; a bare `team 0` still leaves cleanly. ★ **The destructive-tokeniser trap is avoided STRUCTURALLY, not by discipline:** the classifier takes a **`const char*`** and **copies into caller-supplied scratch** before `kv_next` walks it (`:151-158`), and the pre-scan is additionally scoped to the leave form. ⓘ Mutation **C** (tokenise the live tail in place) reddens with **SIGSEGV**. **(b)** `> team new err:` → **`> team err:`** (`:1370`), matching its two siblings; `mobile register`'s three strings untouched. **(c)** ⛔ **The production comment that this change falsified was corrected** (`src/firmware_config.cpp:864-869`): it had recorded the inconsistency as *"preserved rather than tidied (C1)"* — a **known, deliberate** artefact — and now records that B212 makes removing it a **sanctioned fix**, because a message naming a subcommand the operator did not type is a defect. **Gate (QA-gate re-verified: HEAD, classifier, wiring order, prefix, comment, native):** native 1720/83996 → **1722/84041 / 0** (+45 assertions), **four controlled mutations RED** (range-gated classification 15 · "any PHY token" 10 · in-place tokenise 5 + SIGSEGV · empty ⇒ phy_only 5); `probe_prov_tx` 17→**19 checks**, 30→**40 controls RED** (S18/S19 + C30-C39); census 174/178/178 `-Wswitch` 0 unchanged; corpus four-step inert (`b77cfd3d`, 0 actions, recompile control fired, s18 `9868cad3`/269905); `sizeof(Node)` **221880** and `kCap` **91** via a compile-only probe **with its own control**; `git diff -- lib/` **empty**. ⚠ **Cost: RAM +96 B on BOTH ABIs — exactly the `static char phy_scan[96]` scratch**, deliberately static rather than stack (`handle_team` already carries a ~272 B `mrnv::Blob`, and this path has stack-overflow history). Flash +288 / +160 B. ★★ **Two of this slice's OWN structural controls were GREEN=BAD on first run and were REPAIRED, not accepted** — one counted a definition but not the guard using it, the other matched literals in text where literals had been blanked. That is the fourteenth and fifteenth instrument in this arc that could not fail, and the third consecutive slice where the coder's own controls caught them. **Metal residue → bench 27.2's diagnostics half.** |
| B213 | ★★ **OPEN / HIGH / PROVENANCE / METAL-CONFIRMED 2026-08-18** | ⛔⛔ **EVERY `xiao_esp32s3`-FAMILY IMAGE IS UNIDENTIFIABLE FROM ITS OWN BANNER — it reports `nogit`.** Metal: `MeshRoute fw v0.2 · built Aug 18 2026 17:41:05 · **nogit** · board=xiao_esp32s3`. **Verified cause:** `pre:tools/git_rev.py` appears in `platformio.ini` at only **two** sites — `:108` (`[env:xiao_sx1262]`, which `gateway` / `production` / `xiao_mobile` inherit) and `:299` (`[env:heltec_v3]`, which `heltec_mobile` / `gateway_heltec` inherit). ⛔ **`[env:xiao_esp32s3]` is the THIRD BASE BOARD** (`board = seeed_xiao_esp32s3`, `:332`) with **no `extra_scripts` and no `extends`**, so the `#ifndef GIT_REV #define GIT_REV "nogit"` fallback (`src/fw_main.cpp:98`) applies. ⇒ **three envs affected: `xiao_esp32s3`, `gateway_esp32s3`, `xiao_esp32s3_mobile`.** ★★ **THIS IS THE SAME DEFECT [[B200]] FIXED FOR HELTEC, LEFT INCOMPLETE ON THE THIRD BOARD.** `simulation/BASELINE.md` records that fix and its finding — *"`pre:tools/git_rev.py` appeared EXACTLY ONCE … inside `[env:xiao_sx1262]` … ⇒ EVERY HELTEC IMAGE EVER FLASHED HAS BEEN UNIDENTIFIABLE FROM ITS OWN BANNER"* — and the remedy was applied to `heltec_v3` **only**. The identical hole on `xiao_esp32s3` went unnoticed because no provenance-critical work had flashed that family until now. ⚠ **WHY IT MATTERS RATHER THAN BEING COSMETIC: it breaks the archive discipline that made [[B196]] diagnosable.** A panic on such a node cannot be tied to a revision — the banner cannot name one, so an ELF cannot be matched to it except by luck. ⇒ **fix = add `pre:tools/git_rev.py` to `[env:xiao_esp32s3]`**, mirroring `:299`, so its two descendants inherit it. ⚠ **It moves flash bytes (a longer string), not behaviour** — the same note `:290` records for the heltec fix. ⛔ **Until fixed, any `xiao_esp32s3`-family node used in a metal session must have its revision recorded OUT OF BAND**, and that must be written on the result. |
| B214 | ⚠ **OPEN — LABEL FIXED, GUARD OWED / CONSOLE HONESTY / METAL-CONFIRMED** | ⛔⛔ **`cfg` REPORTS `mobile-reg: UNREGISTERED (scanning)` WHENEVER THERE IS NO HOME — even when the mobile FSM is DORMANT and probing for nothing.** `src/firmware_commands.cpp:310` is `if (h) { "REGISTERED home=" } else { "UNREGISTERED (scanning)" }` — the label is derived **solely from whether a home id is set**, and ⛔ **never consults `mobile_attach_state()`**, which is the authority (`dormant` / `seeking` / `claiming` / `attached`). **Metal reproduction 2026-08-18, side by side in one capture:** `cfg` printed `mobile-reg: UNREGISTERED (scanning)` while `mobile status` on the same node reported `"attachment":"dormant"`, `"home_desired":false`, `"last_result":"none"`, `"claim_retries":0`, `retry_window_ms:0` — i.e. **not scanning, no session, no timer armed.** ⚠ **IT ACTIVELY MISLED THE OPERATOR: the owner asked "how often will it probe to find home?" precisely BECAUSE `cfg` said it was scanning. The answer was "never, until a session starts"** — the question was created by the false label. ★★ **Same defect family as [[B210]]** (a `team-DAD` line printed whether or not DAD ran) and the mirror of this project's standing rule that *a display-shaped field must never make an airtime decision* — here a display field **asserts an activity** that is not occurring. ★★★ **THE DEFECT IS BROADER THAN FIRST REGISTERED (QG 2026-08-18), AND THE SECOND HALF IS A FALSE POSITIVE — WORSE THAN THE FALSE "scanning":** ⛔ **`claiming` ALREADY HOLDS A PROVISIONAL HOME ID**, so the `if (h)` test prints **`REGISTERED home=<id>`** *before roster confirmation* — the console claims a registration that has not been confirmed. ⓘ And the first proposed mapping **omitted `recovering`**; the enum has **FIVE** arms (`lib/core/node.h:553-559`: dormant · seeking · claiming · attached · recovering). ⇒ **AUTHORITATIVE MAPPING, from the enum:** `attached` → **`REGISTERED home=<id>`** · `dormant` → `UNREGISTERED (dormant)` · `seeking` → `UNREGISTERED (seeking)` · `claiming` → `UNREGISTERED (claiming)` · `recovering` → `UNREGISTERED (recovering)`. ★★ **AND `attached` WITHOUT a home id must FAIL VISIBLY as inconsistent** — ⛔ never fall through to looking unregistered, which is how a broken attachment would hide. ⇒ derive the label from `mobile_attach_state()` and format it with the existing `Node::attach_state_name()` (U1 — ⛔ do not invent a second spelling; `-Wswitch` covers the enum, and `node.h:550` warns never to rewrite these as if-chains). ⛔ **Pins: (a)** with `autoregister=0` and no session, `cfg` must NOT say "scanning"; **(b)** with an explicit `mobile register` outstanding it must say `seeking`; **(c)** ★ **in `claiming` it must NOT say `REGISTERED`** (the false positive); **(d)** `recovering` must render, not fall into a default; **(e)** `attached` with no home id must report an inconsistency. ⓘ **Coverage is structural** — `src/firmware_commands.cpp` is compiled by neither the native suite nor the simulator; the state names are already natively covered. ⓘ Related but distinct: [[B209]] is about a session being started without being asked; this row is about a session being **reported** that does not exist. ⚠⚠ **PARTIALLY FIXED 2026-08-18 — THE LABEL IS CORRECTED, BUT THE REGRESSION GUARD IS *NOT* BUILT. ⛔ THIS ROW DOES NOT CLOSE.** ⓘ **Provenance, stated plainly so it is weighted correctly: the QA-GATE wrote this code itself** after two coder dispatches died on server-side `529 Overloaded` errors (both verified to have left **zero** partial work). ⇒ ★ **it has NOT been reviewed by anyone but its author — QG should scrutinise it harder than a coder-written slice**, and ⛔ the instruments that would catch a regression are deliberately NOT self-authored (writing the checks for one's own code is the vacuity risk this arc has hit fifteen times). ✅ **What landed:** `src/firmware_commands.cpp:308-337` now derives the label from `mobile_attach_state()` through a **`switch`** over all five arms — ⛔ **no `default:`**, so `-Wswitch` catches a future arm (`node.h:550` records that if-chains are invisible to it and that three enum→string bugs came from exactly that) — with the names from the ONE formatter `attach_state_name` (U1, 2 uses). Mapping as ruled: `attached` → `REGISTERED home=<id>` · the other four → `UNREGISTERED (<state>)`; ★ **`attached` with NO home id prints `INCONSISTENT: attached with no home id`** rather than falling through to looking unregistered. ⓘ Compiles unconditionally: the enum (`node.h:534`) and formatter (`:553`) sit **outside** `#if MR_FEAT_MOBILE` (`:572`), and the accessor's `#else` stub answers `dormant` (`:623`). **Verified:** `xiao_esp32s3` builds **SUCCESS with no new warning**, native **1722 / 84041 / 0** unchanged, and ★ **census PASS at its pins — 174 / 178 / 178, `-Wswitch` 0, 326 objs/env** (the check that actually guards enum exhaustiveness here). ⛔⛔ **STILL OWED, and the row stays OPEN until they exist:** the `tools/probe_console_sink` extension (that probe already reads this file) plus **five controls** — old `if (h)` gate restored · `claiming` mapped to `REGISTERED` · `recovering` dropped to a default · the inconsistency arm removed · the label hand-spelled instead of via `attach_state_name`. Also not run for this change: the other three probes, the four-step corpus proof, the two-env RAM/flash pair, and the compile-only `sizeof(Node)` probe. **⇒ dispatch a coder for the guard when the API recovers.** ⓘ M2 owed: a bench line asserting `cfg` says `(dormant)` with no session and never `REGISTERED` while `claiming`. |
| B215 | ★★ **OPEN / MEDIUM / AIRTIME / MEASURED 2026-08-19** | ⛔⛔ **`reset_join_for_reprovision()` CANCELS ONLY THE CLAIM GUARD — THE STALE LISTEN/RETRY TIMERS STILL PUT A `J_CLAIM` ON AIR.** Found during §UI-15 slice 1's audit (owner disposition: **register, defer the fix**). `lib/core/node_join.cpp:531` cancels `kJoinClaimGuardTimerId` (`:543`) and clears `_joined` / `_join_claim.active`, but leaves **`kJoinListenTimerId` (60)**, **`kJoinRetryTimerId` (59)** and **`_join_listen_pending`** untouched. ⓘ `TimerWheel::after` re-arms by id (`lib/hal/timer_wheel.cpp:8`), so these are **surviving deadlines, not duplicates**. ★★ **MEASURED, not reasoned:** on the `test_node_join.cpp` fixture driving the **real** `TimerWheel`, firing either stale timer after the reset emits `join_claim_sent` and **transmits 1 frame**. **(a) The worst path is `leave`** — `provision_apply_live(b, do_dad=false)` is meant to leave the node **idle-unprovisioned**, but `join_start_claim` guards only on `_joined \|\| _join_claim.active` (`node_join.cpp:161`), **both false after a leave** ⇒ ⛔ **a node told to LEAVE can still claim an id on air up to `join_retry_backoff_ms` = 10 s later.** **(b)** a reprovision after `clear_routing_state()` can claim on an **empty `_id_bind`** table, before the `join_listen_ms` = 3 s window it was owed. ⇒ **fix shape: cancel both timers and clear `_join_listen_pending` in the reset.** ⛔ **OWN SLICE (C1)** — it is a `lib/core` edit and therefore re-runs the corpus, which is exactly why it was NOT folded into slice 1 (that slice's whole claim is byte-identity). ⓘ **Bench owed WITH THE FIX, not before:** `leave`, then confirm **no `J_CLAIM` for 10 s**. |
| B216 | **OPEN / LOW / PRE-EXISTING / MEASURED 2026-08-19** | ⚠ **`freq=nan` AND `bw=nan` ARE ACCEPTED by every PHY-arg call site, and a join proceeds on them.** ⛔ **This row exists because the CODE CLAIMED IT ALREADY DID:** `src/firmware_join_service.h:68` and `test/test_firmware_join_service.cpp:190` both say *"`[[JOIN-BW-NAN]]` is registered"* while the register held **no such entry** — a dangling reference to a registration that was never made (found by QG 2026-08-19). ⓘ The identifier is now **B216**; the two source citations should be updated to it in the next slice that touches those files (⛔ not worth its own slice). ★ **The behaviour is DELIBERATE and documented at `src/firmware_config_parse.h:95-99`:** the range tests are written as **negated rejects** — `!(mhz < 100.0 \|\| mhz > 1000.0)` — and `atof("nan")` yields NaN, for which **both** comparisons are false, ⇒ *"every one of these call sites has always ACCEPTED a NaN. `v >= lo && v <= hi` would start rejecting it — a behaviour change smuggled into a refactor (C1). Preserve it; close it in a fix slice if the owner wants it."* ★★ **MEASURED consequence, from §UI-15 slice 1's A/B harness:** `join layer=4 freq=869.525 bw=nan sf=9` **joins**, persisting `bw_hz = 0` (because `khz_to_hz(NaN)` is 0). ⇒ **a node can be provisioned with a zero bandwidth.** ⓘ **It also constrained the slice-1 design:** pre-converting `bw` to `uint32_t` in the typed request made the NaN indistinguishable from a real 0 and **broke byte-identity**, which is why the request carries `double bw_khz` (plan v6 §2.2). ⇒ **fix shape when ruled: positive-range predicates (`v >= lo && v <= hi`) at the ONE shared authority in `firmware_config_parse.h`**, which fixes every call site at once. ⛔ **Own slice (C2 fail-loud), never folded into a refactor** — that is exactly what the standing instruction forbids. ⛔ No owner ruling exists on whether to close it. ⓘ **UPDATE 2026-08-19 (§UI-15 slice 2): the shared validator is STILL unfixed and the `join` verb's NaN acceptance is unchanged and pinned so** — but `/mrjoin` **refuses** a non-finite at its own boundary (`validate_profile`, `src/firmware_join_profiles.h:145`), because that record is **integral** and `static_cast<uint32_t>(NaN)` is **undefined behaviour**. ⇒ the divergence is deliberate and legitimate: the profile store is **new surface with no byte-identity obligation**. |
| B217 | ✅ **CLOSED 2026-08-21 / GATE DEFECT / QG-PASSED (three rounds) · ⛔ UNCOMMITTED** | ⛔⛔ **A SLICE THAT CHANGES THE NATIVE COUNTS SILENTLY DISARMS EVERY MUTATION BATTERY PINNED TO THEM — AND THIS ALREADY HAPPENED.** `tools/probe_ui_model_mutations.py` gates on a hard-pinned clean baseline (`BASE_CASES, BASE_ASSERTS`, `:77`) and, on any mismatch, prints `ABORT the clean baseline is …` and **`sys.exit(2)` WITHOUT APPLYING A SINGLE MUTATION** (`:1039-1043`). ★★ **MEASURED: §UI-15 slice 1 added `test/test_firmware_join_service.cpp` (1680/83432 → 1733/84164) and did NOT re-pin ⇒ from that moment EVERY invocation for ALL FOUR existing targets ran ZERO mutations.** Found by slice 2, which re-pinned to **1754/84519** with the derivation recorded in place. ⓘ **Exposure was narrow in practice** — slice 1 used its own A/B harness and B214's coverage was structural in `probe_console_sink` — but ⛔ **the CLASS is what matters, and it is the worst shape this arc has recorded: it does not produce a false GREEN, it produces NO MEASUREMENT AT ALL, across four batteries at once**, behind an exit code a careless reader takes for "ran, nothing wrong". ⇒ **this is the eighteenth instrument-that-cannot-fail in this arc and the first that disarmed OTHER instruments.** ★ **Required correction, and ⛔ re-pinning by hand is NOT it** — that is what failed: (a) make the harness **derive** the baseline from a clean run instead of a literal, **or** (b) make a stale pin **loud and unmistakable** (⛔ not exit-2-and-quiet), **or** (c) add a standing gate step that re-pins whenever native counts move. ⓘ `MR_MUT_BASE` (`:128`) already allows an env override — a hook a derived-baseline fix could reuse. ⚠ **Until fixed, EVERY brief that relies on this harness must state the current native counts and require the coder to confirm the battery actually RAN** (a battery that aborted reports no RED lines — which reads exactly like a battery with nothing to find). ✅ **CLOSED 2026-08-21, QG PASS after THREE rounds — shape (a)+(b):** the baseline is **DERIVED from the clean run every invocation** (0-failed still aborts LOUDLY with the failing output); the hand figure survives only as the **`PIN_CASES` optional cross-check** — renamed so no reader re-installs the defect — warning unmissably at BOTH ends and NEVER stopping a battery; the exit code stays the mutations' verdict. QG's probing of the fix surfaced and closed **two sibling silent-zero paths**: unknown/mistyped argv (⇒ [[B235]], exit 7; unknown target exit 6) and a **zero-match label prefix** (`ZZZ` ⇒ was exit-0 "0 RED"; now **exit 8**, refused before any build), plus a false "runs in full" banner claim on filtered runs (now the banners name the selection truthfully). **MEASURED:** the pre-fix defect invocation reproduced (exit-2, zero mutations) then re-run under the fix (full battery + banner); ten-target sweep **286/286 RED / 0 unusable**, every count identical pre/post; native unmoved 1861/87209/0. ⇒ **THE RE-PIN DUTY IS RETIRED from every future brief** — replace it with: keep the `PIN_*` cross-check current when counts move (it can no longer disarm anything, only warn — don't normalise the banner), and keep "confirm the battery RAN". |
| B218 | ✅ **CLOSED 2026-08-19 (reopened + re-fixed same day) / HONESTY / QG-PASSED · ⛔ UNCOMMITTED** | ⛔⛔ **`/mrjoin` REPORTED A STORAGE FAILURE AS `NO PROFILES`.** Both live `read_slot` arms folded *"the backend would not open"* into the same `-1` used for *"no such record"* — nRF52 **discarded `InternalFS.begin()`'s return**, ESP32 returned `-1` on `!p.begin()` — and `join_blob_state` mapped `-1` to `absent` **unconditionally**. ⇒ **a filesystem that would not mount announced `NO PROFILES`**, defeating the record's central honesty requirement **one layer beneath the service that implements it**. ★ **FIX: a `SlotIo` out-param on the SHARED primitive** (`src/device_nv.h:326`) — chosen over a forked `/mrjoin`-only reader because that would duplicate the primitive and require parallel maintenance forever (U1). ★★ **Safety for the other four records is MEASURED, not argued:** the `int` return is unchanged on every path of every arm, `io` defaults to `nullptr`, and every extra backend question is asked **only when it is non-null** ⇒ `/mrcfg`, `/mrid`, `/mrpeers`, `/mrfault` issue an identical call sequence — pinned by a zero-extra-calls case and by mutations **N11/N12**, which make the question unconditional and go RED. ★★★ **A FOURTH STATE, NOT A THIRD — and the distinction is the point:** `invalid` = *"I read it and it is wrong"* (repairable by `reset confirm`); **`io_failed`** = *"I could not read it at all"*, where `reset confirm` **would destroy four possibly-intact profiles to 'repair' a fault it cannot repair** ⇒ **all four verbs refuse with ZERO writes on `io_failed`** (`ProfileErr::store_io_failed`, `src/firmware_join_profiles.h:106`). ⚠ **AND A SECOND DISHONESTY, POINTING THE OTHER WAY, THAT THE BRIEF DID NOT NAME:** on ESP32 `Preferences::begin(ns, readOnly)` returns the same `false` for *"this namespace has never been written"* (an ordinary fresh chip) as for *"NVS would not open"* ⇒ treating both as failure would make **a FRESH CHIP report STORAGE FAILURE**. The arm now classifies with a **read-only `nvs_open`** and `ESP_ERR_NVS_NOT_FOUND` (`src/device_nv.h:636-638`) — ⛔ never a read-write probe, which would **create the namespace: a flash write inside a read**. Mutation **N03** reddens the unclassified version. **MEASURED:** native 1754/84519 → **1764/84705**; **8 mutations RED at match count 1**. ⛔ Adapter residue stated, not glossed — see [[B221]]. ★★ **REOPENED 2026-08-19 (QG): the nRF52 fix stops ONE LAYER SHORT.** `fs_read_slot` still maps EVERY `open()==false` to `absent` (`src/device_nv.h:364`), and the vendored `File::_open` (`Adafruit_LittleFS_File.cpp:121-153`) answers that same `false` for a `lfs_stat` METADATA ERROR, an ALLOCATION FAILURE and a `lfs_file_open` FAILURE as for NOENT — it stats first and only NOENT means "no such file" ⇒ **a damaged-but-mountable `/mrjoin` still announces `NO PROFILES`**; only the mount failure was fixed. **Required: a tri-state lookup at the nRF boundary — only `LFS_ERR_NOENT` is `absent`; a lookup/metadata error, and present-followed-by-open-failure, are `io_failed`** — with the `io == nullptr` call sequence for the other four records unmoved, and controlled cases for all three outcomes. Correction brief: `docs/superpowers/plans/2026-08-19-ui15-slice2-b218-reopen.md`. ✅ **RE-CLOSED 2026-08-19, QG PASS:** tri-state landed — only `LFS_ERR_NOENT` is `absent`; a stat metadata error, and present-followed-by-open-failure, are `io_failed`; the legacy (`io == nullptr`) records perform **no added lookup** (counted). **MEASURED:** native **1764/84729/0** · device-NV battery **15 RED / 0 unusable** (was 12) · both live arms compile · boards SUCCESS · s18 `9868cad3`/269905 · `sizeof(Node)` 221880. |
| B219 | ✅ **CLOSED 2026-08-19 / VACUOUS-INSTRUMENT (the NINETEENTH in this arc) · ⛔ UNCOMMITTED** | ⛔ **AN OVER-LENGTH `/mrjoin` RECORD WAS ACCEPTED ON ITS VALID PREFIX.** `read_slot` reads at most `len`, so a longer file returns exactly `sizeof(JoinBlob)` and `join_blob_state` accepted the prefix; there was **no size check**. ★★ **AND THE COVERAGE WAS VACUOUS — this is the part worth remembering:** the native `over-long` subcase asserted `join_blob_state(b, sizeof b + 1)`, **a length NO BACKEND CAN EVER RETURN**. It could not have failed, and it made the gap look covered. ★ **FIX:** `SlotIo::oversize` from `File::size()` (nRF52) and `getBytesLength()` (ESP32) — the ESP32 flag is explicit because `getBytes` already answers 0 there; `join_blob_state` maps it to `invalid`. ★★ **The vacuous subcase was DELETED, not moved**, and replaced by a **real over-length file** (`file_size = sizeof(JoinBlob) + 40`, read returns exactly `sizeof(JoinBlob)`) carrying its own control: **without the size, that same read IS a perfect record**. **MEASURED: 4 mutations RED.** |
| B220 | ✅ **CLOSED 2026-08-19 / C2 · ⛔ UNCOMMITTED** | ⛔ **`joinprofile` VIOLATED ITS OWN MALFORMED-INDEX RULE.** `atol` at both sites ⇒ **`clear 2junk` cleared slot 2** and **`set 1x …` overwrote slot 1** — both **WRITES** — while `list extra`, `clear 1 extra` and `reset confirm extra` **silently dropped the trailing token**. The slice's own contract said *a malformed or out-of-range index refuses loudly and writes nothing*. ★ **FIX:** `parse_index_strict` + `arg_tail_empty` extracted to the **pure** `src/firmware_config_parse.h:127/143` — ★★ **the [[B212]] move for the third time in this arc, and for the same reason: `src/firmware_config.cpp` is compiled by NEITHER the native suite NOR the simulator, so a parser left there cannot be shown to DO anything** (a structural probe can only show that text exists). Refusals are voiced through the **same reporter** as `clear 0`, so a mistyped index and an out-of-range one read identically, and the service stays the one range authority. ⓘ Each native case is paired with **what `atol` answered**, because "strict" is only meaningful against the leniency it replaces. **MEASURED: 8 mutations RED.** |
| B221 | **OPEN / LOW / COVERAGE RESIDUE / 2026-08-19** | ⚠ **THE NV ADAPTERS ARE COMPILED BY NO AUTOMATED GATE.** `InternalFsSlot` and `PreferencesSlot` (the five-member shims) and the real `nvs_open` call are exercised by neither the native suite nor the simulator. ⓘ **They hold NO branch of their own** — every decision lives in the hoisted, host-driven `mrnv::fs_read_slot` / `mrnv::nvs_read_slot` sequences, which the native suite drives against a fake FS / fake NVS that fails at `mount()` / `open()`. ⇒ **the residue is exactly "the adapter forwards to the right library call", which remains A READING, NOT A MEASUREMENT.** ★ Reported by the coder rather than glossed — the honest form. ⇒ **closable only on metal** (bench 28.4/28.5, [[B218]]'s fresh-chip and unreadable-store lines), or by a mechanical check that the shim's member names match the library's. ⛔ No owner ruling exists. ⚠ **AMENDED 2026-08-19 ([[B218]] reopen): "they hold NO branch of their own" UNDERSTATED the residue.** A shim member's SIGNATURE can itself discard the distinction the hoisted sequence needs — `bool open()` folded NOENT and every IO error into one `false`, and no hoisted logic can recover a fact the boundary already erased. The residue is therefore not only *"the adapter forwards to the right call"* but *"the member's shape carries ENOUGH of the library's answer"* — a reading the B218 reopen shows can be wrong. |
| B222 | ✅ **CLOSED 2026-08-19 (same day) / SCOPE / QG-PASSED · ⛔ UNCOMMITTED** | ⛔ **§UI-15 SLICE 4 LANDED TWO SLICE-5/6 TRANSITIONS AHEAD OF THEIR SLICES.** The brief permitted only gated entry, `closed ↔ menu` and close-on-leave; the menu activations **`CREATE TEAM → create_confirm`** and **`JOIN NETWORK → join_select`** (`src/firmware_ui_model.h:1942-1943`) are the child FLOW entries the brief deferred, and their flow tests rode along (`test/test_firmware_ui_model.cpp:3238`). ⚠ Not a behaviour defect — the transitions are the PLANNED ones — but a scope defect: slices 5/6 lose the clean landing the plan's per-slice gating depends on, and an unrenderable state becomes reachable one slice early. **Required (QG-ruled): keep the eight states, `ProvRow` model and labels; MOVE the two transitions + their flow tests to slices 5/6; test the BACK confirm-default directly as model state.** ✅ **CLOSED 2026-08-19, QG PASS:** activation of both rows is now a NO-OP (asserted by `ui15-pending`: cursor unmoved, no block, no note, zero writes), the pending half stated in-source in three places naming slices 5/6; `provision_confirm_activate()` deleted as dead; the BACK default asserted directly as model state (M62 re-covered). **MEASURED:** native **1783/85884/0** · model battery **67 RED / 0 unusable**, kept entries all match count 1. |
| B223 | ✅ **CLOSED 2026-08-19 (same day) / VACUOUS-INSTRUMENT (the TWENTIETH in this arc) / QG-PASSED · ⛔ UNCOMMITTED** | ⛔⛔ **THE REQUIRED CLOSE-ON-LEAVE REGRESSION GUARD DOES NOT GUARD THE RESET IT WAS REQUIRED TO GUARD.** The brief required *"leaving SETTINGS from ANY provisioning arm returns `Provision::closed` — mutation dropping the reset goes RED"*. As landed: `settings_follow_screen()`'s reset is **UNREACHABLE and carries no mutation** — stated in-source (`src/firmware_ui_model.h:1721-1730`), the honest form, but stating a gap does not discharge it; **M54 mutates `close_provisioning()`, a DIFFERENT path** (`tools/probe_ui_model_mutations.py:669`); and the test titled *"walking OFF the SETTINGS screen closes provisioning"* **closes the sub-view BEFORE leaving SETTINGS, so it cannot exercise the reset it names** (`test/test_firmware_ui_model.cpp:3293`) — the instrument-that-cannot-fail shape, one test at a time. Only 3 of the 8 arms were checked. **Required (QG-ruled): extract the off-screen reset into a small PURE helper used by `settings_follow_screen()`; drive ALL EIGHT arms through the helper directly; mutate the helper's actual `Provision::closed` assignment — RED at match count 1.** ⓘ The pure-extraction move is [[B212]]/[[B220]]'s, for the fourth time. ✅ **CLOSED 2026-08-19, QG PASS:** the reset hoisted into pure `provision_reset_on_leave(Provision&, ProvConfirm&)` (`src/firmware_ui_model.h:235`), BOTH leave paths now forwards to it (U1); the suite drives **all 8 arms × both cursor values + idempotence** directly; **M67 mutates the guard's own assignment** (the "only the reachable arm needs closing" form) — **RED at match count 1**; the test title that claimed a path it did not take is retired; M54 kept as the labelled reachable-path control. **MEASURED:** native **1783/85884/0** · **67 RED / 0 unusable**, restore md5 matched. |
| B224 | **OPEN / LOW / DECLARED DUPLICATION / 2026-08-19** | ⚠ **`prov_device_facts()` (`src/firmware_config.cpp`, §UI-15 slice 5) IS A SECOND COPY of `handle_team`'s live-snapshot fill — declared, reasoned, and ⛔ deliberately NOT merged.** Sharing was C1-blocked: `probe_prov_tx`'s **S11 pins those six assignments INSIDE `handle_team`'s body**, so routing the console through a helper would redden a probe for a refactor the slice may not make. The reason is written at the function; **the two copies must be edited together** (stated in-source). ⓘ Found the hard way: an identically-spelled out-parameter made mutation controls C12/C13/C13b **UNUSABLE at match count 2** — the rename to `out` is load-bearing, not style. **CLOSE BY:** a deliberate refactor slice that moves S11's pin and merges the two fills into one helper (the [[duplicated-code]] first-class-work rule applies; it is not shelvable polish). |
| B225 | ✅ **CLOSED 2026-08-20 (next day) / COVERAGE GAP / QG-PASSED · ⛔ UNCOMMITTED** | ⛔ **THE PRODUCTION PROVISIONING RENDERER WAS UNPROBED THROUGH THE CHILD-ENABLED FLOW.** `draw_provision_screen` (`src/firmware_ui.cpp:1143`) is real shipped rendering, and `probe_firmware_ui` — the ONLY automated cover `firmware_ui.cpp` has — compiled the TU only with **`-DMR_N_LAYERS=2` (the LAYERED/GATEWAY shape — ⚠ its comment miscalled it a `heltec_v3` mirror, a drift corrected with the fix), the one configuration in which BOTH provisioning children hide**, so the slice-5 renderer arm was structurally unreachable in the probe; the slice added only the hidden-parent check (P7). **The slice-5 brief's item 6 ("extend probe expectations for the new screens") was therefore satisfied in name only** — model/chrome tests cannot see an omitted, swapped or mis-wired draw call in the production TU. **Required (QG-ruled): a CHILD-ENABLED feature-probe arm** (a second compile mirroring a REAL leaf env's `-D` set — the probe's own drift warning forbids an invented configuration) exercising **menu → CREATE TEAM confirm → result**, pinning: BACK default + ZERO transaction/write/apply · the optional `REPLACES <fp>` warning · success output (`TEAM CREATED`, full id, SHARED fingerprint, back instruction) · at least PHY-mismatch and persistence/refusal results · **plus controls that remove or swap those renderer lines** (the probe's tempting-wrong-fix discipline). ✅ **CLOSED 2026-08-20, QG PASS:** the probe now runs TWO arms — **`l2`** (the layered/gateway shape, unchanged, 230/0) and **`v3`** (the `heltec_v3` **UI-relevant** configuration — `MR_N_LAYERS` unset ⇒ 1; radio/board/telemetry flags intentionally omitted as read by no TU in the link — **274/0**), each with its own support archive (`MR_N_LAYERS` sizes `Node::_layers[]`). The `v3` arm drives the **REAL `UiProvisionAdapter` + real transaction** (only the three device forwards faked; control L8 proves the attach); the fingerprint is pinned as a VALUE relation at its own row (a `strstr` would match inside the full id). **MEASURED:** controls **100 verified / 0 unusable** (10 new L1-L10, all three worthlessness checks applied) · native unmoved 1800/86200/0 · no renderer defect found (the initial reds were harness frame-timing, documented at the probe's `see()` helper). |
| B226 | ✅ **CLOSED 2026-08-20 (same day) / VACUOUS COVERAGE (the TWENTY-FIRST) / QG-PASSED · ⛔ UNCOMMITTED** | ⛔ **`STILL JOINING` IS UNPROVED THROUGH THE REAL RENDERER.** The pure model tests the 60 s latch (`UiState::join_still`, `src/firmware_ui_model.h:1286`), and the renderer arm consumes it (`join_wait_head(st.join_still)`, `src/firmware_ui.cpp:1277`) — but the probe's P16 phase renders only the immediate `JOINING` and then advances time in small steps (`probe_main.cpp:2355`), so **hard-wiring `join_wait_head(false)` in production would likely leave every current gate green.** **Required (QG-ruled) probe sequence:** advance past 60 s → the panel BLANKED but the session still active → ONE short press consumed as wake → the REAL renderer shows `STILL JOINING` → zero additional writes/applies → **a renderer control hard-wiring `false` / inverting `st.join_still` goes RED.** ✅ **CLOSED 2026-08-20, QG PASS:** the six-step P16f sequence landed exactly as ruled (`probe_main.cpp:2456`) — blank-with-session-alive measured ON THE STORE, the wake press proven consumed, the REAL renderer shows `STILL JOINING`, counters frozen — and **L21** is deliberately the QUIET surviving-mutant shape (`join_wait_head(false)`, which every other gate survives) → **RED**. **MEASURED:** v3 arm 318/318 · 112 controls / 0 unusable. |
| B227 | ✅ **CLOSED 2026-08-20 (same day) / GATE INTEGRITY / QG-PASSED · ⛔ UNCOMMITTED** | ⛔⛔ **THE FEATURE-PROBE RUNNER EXECUTED A CONTROL LABEL AS SHELL.** The L19 label carries `` `/mrcfg` `` inside double quotes (`tools/probe_firmware_ui/run.sh:730`) ⇒ command substitution ran `/mrcfg`, the gate printed `/mrcfg: No such file or directory` **and still returned PASS**. A harness that executes its own labels as code can be broken by a LABEL EDIT — the gate's output stream carried an error the exit code ignored. **Required: labels handled as inert data** (at minimum the backticks removed; ideally the runner prints labels un-evaluated so no future label can execute). ✅ **CLOSED 2026-08-20, QG PASS — TWO HALVES, because a printer cannot undo a call-site expansion:** (1) a self-audit over the runner's own `ctl` lines — no backtick, no `$`, with TWO vacuity guards (extracted count must equal call count, 112=112, and the detector must fire on a synthetic hazard); (2) every label printed via `printf '%s'` of an unexpanded variable (`run.sh:188`), proven by a backtick-and-`$HOME`-bearing label printing VERBATIM against an independently-spelled literal, stderr empty. L19 was the only hazard of 112; the sibling board probe audited (backticks in comments only). The stray `/mrcfg` error line is GONE from the gate output. |
| B228 | ✅ **CLOSED 2026-08-20 (same day) / EFFICIENCY-ON-HOT-PATH / QG-PASSED · ⛔ UNCOMMITTED** | ⚠ **EVERY push during an active join session costs a FLASH READ before the kind gate rejects it.** `ui_join_note_push` (`src/firmware_ui.cpp:1796`) loads the persisted `/mrcfg` Blob and only then hands the pure rule the push — whose kind gate rejects every non-adopt. The session survives leaving the waiting screen INDEFINITELY (the declared slice-6 behaviour), so the in-source "a minute or two" cost claim is wrong (a drifted comment carrying a false bound — the standing V1 class). **Required: a cheap kind prefilter (`pu.kind != join_adopted ⇒ return`) BEFORE the load, the four-term rule staying the sole authority after it, plus a probe showing an active session + `join_refused`/unrelated pushes performs ZERO record reads.** ✅ **CLOSED 2026-08-20, QG PASS:** the one-line kind prefilter sits BEFORE the load (`src/firmware_ui.cpp:1801`), grows no term; P16e fires all four refuse reasons + six unrelated kinds under an active session with **zero record reads (counted)**; control **L22** (prefilter dropped) RED; **all 25 `uijoin` rule mutations still RED** — the prefilter weakened nothing. The false "a minute or two" bound corrected in place (V1). |
| B229 | ✅ **CLOSED 2026-08-21 (§UI-17 S4) — THE MECHANISM, not the four labels / QG-VERIFIED** | ⚠ **THE FEATURE-PROBE ROLL-UP UNDER-ATTRIBUTED CONTROLS FOR LABELS OVER 64 CHARACTERS.** `run.sh` re-reads a `FAIL` line with `s/^  FAIL \(.\{1,64\}\)  .*$/\1/` — which needs the `%-64s` padding to find the label's end, so **a check whose label exceeds 64 chars is reported `(no control reddens)` even when a control DOES redden it** — an under-count in the attribution roll-up, ⛔ not a false PASS. Three new labels tripped it (coverage jumped 265 → 267 when shortened); the bound is now stated at the `CHK` macro. ⚠ **Residue, stated:** two PRE-EXISTING labels still exceed the bound (`P7b …withdrawn marker TEXT`, `P14d precondition:`) — both genuinely un-reddened today, so nothing is currently mis-attributed; left under C1. **CLOSE the residue by** shortening them (or making the roll-up length-proof) in the next probe-touching slice. ✅ **CLOSED 2026-08-21 (§UI-17 S4), QG-VERIFIED — the roll-up is now LENGTH-PROOF:** `run.sh` attributes a reddening via `attribute()` — longest-prefix match against the arm's own `PROBE_LIST=1` label list — with an **in-run vacuity guard: a synthetic 91-CHARACTER label must attribute, and the WITHDRAWN 64-column parse is required to MISS it.** **MEASURED:** 7/7 over-64 labels in the tree attributed, **6 previously missed outright** (incl. `P7b …withdrawn marker TEXT`, which C84 reddens); coverage honestly **334 vs the old parse's 328** (later 335 after [[B237]]'s C81 fix); the byte-vs-character trap named and closed. ⚠ **The row's own earlier sentence "both genuinely un-reddened today, so nothing is currently mis-attributed" had gone STALE — C84 was reddening P7b since S3 re-pointed it**; corrected here rather than left as history claiming to be present. |
| B230 | ✅ **CLOSED 2026-08-20 (same day) / UX DEAD-END + SELF-CONTRADICTORY DIAGNOSTIC / QG-PASSED · ⛔ UNCOMMITTED, metal recheck = bench 27.17** | ⛔ **A NODE WITH AN UNSET `sf_list` CANNOT REACH `team new` AT ALL, AND THE REFUSAL'S OWN REMEDY IS A COMMAND THAT FAILS THE SAME CHECK.** **MEASURED (owner console):** `team new freq=869 sf=7 bw=125` ⇒ `> team err: incomplete PHY — need freq, routing_sf(5..12), sf_list(DATA SF), bw.` + `>   set them inline: \`team new freq=869.0 sf=7 bw=125\` …` — **the suggested line IS the line that just failed**; and `team new freq=869 sf=7 bw=125 sf_list=7` ⇒ `> team err bad/unknown key: sf_list`. **MECHANISM (verified at code):** `ProvErr::incomplete_phy` (`firmware_config.cpp:1539-1542`) requires a non-empty staged `sf_list`; the persisted one is EMPTY after `factory_reset`; and the `team new` PHY tail **deliberately carries no `sf_list` key** — that omission is [[B211]]'s fix (`:1734-1742`: team ops preserve the persisted list, `0` = "operator named none"). The two deliberate rules compose into a dead-end: the ONLY escape is `cfg set sf_list <…>` first, which nothing tells the operator. ⓘ **The refusal itself is CORRECT (C2 / the data-sf-removed ruling: an empty `sf_list` must refuse, never default)** — the defect is the REMEDY TEXT (names `sf_list` as if inline-settable, suggests a command that cannot succeed). ★ **Fix shapes, owner to rule:** (a) **cheapest, message-only** — the `incomplete_phy` text says which part is missing and, when it is `sf_list`, points at `cfg set sf_list 6,7` (⛔ not an inline key); (b) accept `sf_list=` in the `team new` tail — ⚠ this touches [[B211]]'s deliberate omission and needs its own ruling, ⛔ not to be done casually. ⓘ `create` (the static verb) already accepts `sf_list=` inline (`:1136`) — the asymmetry is why the operator reasonably tried it. **WORKAROUND for the live session:** `cfg set sf_list 6,7` then `team new …`. ★ **OWNER RULED 2026-08-20 (reported form): shape (a) — MESSAGE-ONLY.** The `incomplete_phy` refusal names WHICH part is missing and, when it is `sf_list`, points at `cfg set sf_list 6,7` (⛔ never presented as an inline key); [[B211]]'s tail omission stands untouched. Queued behind the [[B231]]/[[B233]] dispatch. ✅ **CLOSED 2026-08-20, QG PASS (after one QG-caught correction):** shape (a) landed as a distinct **`ProvErr::sf_list_empty`** arm (classification native-tested in the service; 13 chars so the §7.3 panel audit holds; the arm speaks FIRST wherever the inline remedy would lie). ⚠ **QG caught the fix repeating the defect's shape one layer over:** the first remedy said "then `team new …`" although the arm fires for ALL `team` forms and `ProvResult` records none — a JOINER would have been told to mint a random team. **Both arms are now FORM-NEUTRAL** ("retry your original `team` command" / "set them inline on your `team` command: `freq=… sf=… bw=…`"), with S20 asserting `team new` occurs ZERO times in both arm windows and controls **C45/C46** mutating the defect back verbatim. **MEASURED:** native **1837/86986/0** · new `provservice` battery **4 RED / 0 unusable** · `probe_prov_tx` **20/20 + 47 controls RED** · `prov_err_name` all-enumerators census case created · zero outcome changes (write/live counted on every block). Metal recheck = bench **27.17** (incl. the form-neutrality JOIN arm — the check a `team new`-only test misses). |
| B231 | ✅ **CLOSED 2026-08-20 (same day) / UX / QG-PASSED · ⛔ UNCOMMITTED, panel recheck owed (Part 31)** | ★ **OWNER RULED: the INBOX list shows messages NEWEST AT THE TOP.** As built it is the opposite WITHIN each block: `InboxRowBudget` correctly keeps the newest `kInboxRowsPerKind` per kind (newest-wins eviction), but `publish` (`src/firmware_ui_model.h:823-829`) copies each ring in stored order — which is `Inbox::pull()`'s **oldest-first** — so the newest message renders at the BOTTOM of its block. **FIX SHAPE (cheap, pure, host-tested):** publish each ring in REVERSE (newest first); the eviction shift and the one-conversion-path (U2) stay as they are. ⚠ **SCOPE BOUNDARY, stated so the fix does not overreach:** the BLOCK order (all DM rows, then all channel rows, ⛔ never chronologically interleaved) is a DOCUMENTED spec §6.1 decision — the two seq spaces share no clock, and interleaving needs a stated reboot/uptime rule first (`:802-804`). Newest-first WITHIN each block needs no such rule and does not touch it. ⓘ Ripple to check in the fix: the INBOX cursor's identity tracking (`sync_inbox_cursor`) across the reordering, and the detail modal opening the row the cursor names — both host-testable. ✅ **CLOSED 2026-08-20, QG PASS:** `publish` copies each ring in REVERSE; block order untouched. Mutations **M92/M93** (per-ring reversal undone/half-applied) RED at match count 1; the §B64 cursor rule re-proved from the NEW direction (arrivals now push rows DOWN — a newcomer above the highlight must not steal it). **MEASURED:** native **1835/86848/0** · model battery **96 RED / 0 unusable** · probe `l2` 232 / `v3` 320, 112 controls. Panel recheck = bench **Part 31**. |
| B232 | ✅ **CLOSED 2026-08-20 (same day) / UX (UI CHANGE) / QG-PASSED · ⛔ UNCOMMITTED, metal recheck = Part 20/25.4 press-sequence re-run** | ★ **OWNER RULED: the SETTINGS screen presents ONE visible entry ("enter settings"); the operator must NOT have to walk every option row just to pass the screen.** As built: `sync_settings` **auto-enters `browsing` on ARRIVAL** (`src/firmware_ui_model.h:2036`, `Settings::closed → browsing` on the first tick), and `advance_or_next` (`:1947`) moves to the next screen only after the cursor walks off the LAST row ⇒ **cycling past SETTINGS in the STATUS→TEAM→INBOX→SEND→SETTINGS loop costs a short-press per row — up to 9** (BLE/DM crypt/key attach/auto reg/PROVISION/[RELOAD]/SAVE/DISCARD/BACK). Every other screen passes in ONE press. **Ruled shape: SETTINGS lands CLOSED with a single entry row; `short` passes to the next screen; `double` enters `browsing`** (the same enter-by-double idiom the PROVISION child menu already uses). ⚠ **NOT a casual patch — a small slice with its own ripple list:** (1) ⛔ the ConfigService must STILL open on arrival (`:2041`) — the §3.6.1 baseline/conflict latch and the rail badge depend on it, and the badge table's *"SETTINGS must ALSO print `CFG* UNSAVED` / `CFG! RELOAD`"* must remain satisfied from the CLOSED single-entry view (an icon-only error is design §6's forbidden state); (2) close-on-leave (`settings_follow_screen`) and the [[B223]] guard are entry-state-dependent — re-verify their mutations; (3) the walkthrough/bench Part 25.4 badge rows and Part 20's SETTINGS flows change their press sequences — the bench script inherits an edit; (4) it REVERSES a §UI-14 documented behaviour ⇒ the design doc's SETTINGS section needs the correction-in-place. Host-testable end to end (`model` battery + the probe's SETTINGS phases). |
| B233 | ✅ **CLOSED 2026-08-20 (same day) / STALE RENDER AFTER DELETE / QG-PASSED · ⛔ UNCOMMITTED, panel recheck owed (Part 31)** | ⛔ **AFTER DELETING A MESSAGE, THE INBOX LIST STILL SHOWS THE DELETED ROW — INDEFINITELY — AND `double` ON IT OPENS THE *NEXT* MESSAGE.** **MECHANISM (verified in `src/firmware_ui.cpp`'s tick ordering):** the tick builds the snapshot FIRST (`build_snapshot`, `:1377` area — `fill_inbox_rows` re-pulls fresh rows, `:613`), the gesture then queues the erase, `ui_service_inbox_request` performs it MID-TICK (`:1643`, deliberately "before the frame gate … or the press would appear to do nothing") — **but the frame that then freezes renders the PRE-ERASE snapshot**, whose `s.inbox[]` still carries the deleted row, **and that frame CONSUMES the model's `dirty`**. Next tick pulls fresh rows (tombstone-filtered, `inbox.h:132-137`) but the model is now CLEAN ⇒ `FrameGate::step` answers idle ⇒ **no repaint ever comes**: the artefact persists until an unrelated event dirties the model. The `:1641` comment solved the `UiState` half of exactly this staleness and missed the SNAPSHOT half. Meanwhile the model's identity-tracked cursor correctly follows the NEIGHBOUR captured before the delete (`note_inbox_neighbour`) ⇒ the stale row's text and the row's real identity disagree — the mis-open the owner observed. ★ **Fix shapes:** (a) a serviced inbox MUTATION leaves the model DIRTY FOR ONE MORE FRAME (a needs-fresh-snapshot latch — the next tick's fresh pull then repaints; cheapest, pure, mutation-testable "dirty survives the stale frame"); or (b) re-fill the snapshot's inbox rows after the drain, before the freeze (zero-artefact but re-orders the tick — check the `:1641` rationale and the frozen-chrome rule). ⓘ Either way the regression case is host-drivable: erase → next frame's rows must not contain the erased identity. ⚠ Same class as [[B231]]'s ripple — one dispatch can carry both list fixes. ⓘ **OWNER OBSERVATION (same session): deleting the LAST message of the list refreshed CORRECTLY.** Consistent with the mechanism, as a HYPOTHESIS the fixer must confirm: the per-tick `sync_inbox_cursor` re-anchors by IDENTITY against the fresh snapshot — when the last row was deleted the remembered pick/neighbour identity no longer exists ⇒ the identity tracker notices, sets `dirty`, and the NEXT frame repaints from fresh rows; a middle-row delete leaves the cursor on a still-valid neighbour ⇒ nothing re-dirties and the stale frame stands. ⇒ the artefact hides exactly when the post-delete cursor identity is valid — the regression suite must drive BOTH arms (delete-middle: stale today; delete-last: refreshed today; after the fix BOTH refresh, and the delete-last case guards against the fix regressing the working arm). ✅ **CLOSED 2026-08-20, QG PASS — shape (a):** a success-only one-shot `_inbox_rows_stale` latch raised on the `erased` arm, consumed by the next `on_tick` into `dirty` ⇒ the following tick's existing fresh pull repaints; **`pulls == ticks` COUNTED** in all three cases (zero extra store traffic; shape (b) would read one more). Delete-middle reproduced RED against the UNFIXED tree first (the frozen frame still carried the row 2.4 s later; no ninth frame ever opened); delete-last guarded (M95). Mutations **M94/M95/M96** RED at match count 1; the probe's `v3` arm gained the on-glass *"the list catches up with NO further press"* check (hand-verified non-vacuous). ⚠ **THE HYPOTHESIS WAS CONFIRMED IN OUTCOME BUT ITS MECHANISM CORRECTED (measured):** delete-LAST refreshes ⛔ NOT because the identity vanishes — the fallback neighbour stays valid — but because the predecessor lands at a DIFFERENT INDEX, and `sync_inbox_cursor`'s **index-move branch** (`_st.cursor != i`) is what re-dirties; a middle delete leaves the neighbour at the SAME index, so nothing notices. Both facts asserted in cases, not narrated. **MEASURED:** native **1835/86848/0** · 96 RED / 0 unusable. Panel recheck = bench **Part 31**. |

| B234 | ✅ **CLOSED 2026-08-20 — ALREADY SATISFIED (owner re-ruled: UI-ONLY was the intent; the row overstated the ruling)** | ★ **LEAVING OR REPLACING A NON-ZERO TEAM REQUIRES EXPLICIT CONFIRMATION.** This is not an OLED-only feature: today UI-15's CREATE/JOIN flows have BACK-default confirmations, but the serial/BLE console can execute the same membership change directly. The reliable rule is deliberately stronger than testing `rt_team_count() > 0`: that table is transient and its absence cannot prove the mobile is the team's only member—especially after connectivity is lost. Therefore guard every operation whose committed candidate changes `current_team_id != 0` to a different id or zero. This includes `team new`, `team <different-id>`, `team 0`, and `leave`; `factory_reset confirm` already satisfies the shape. Same-team re-key/PHY/no-change does not leave and must not be gated. **Required shape:** one shared provisioning/command predicate before entropy, NV write, live apply or airtime; typed request carries explicit confirmation; console/BLE use a stateless terminal `confirm` token (never echo a command containing private key material); OLED may satisfy the same typed flag only after its existing BACK-default confirmation. First unconfirmed attempt refuses loudly and changes nothing. Tests must cover alone/no-route as well as known-peer states, proving that route expiry cannot remove the guard, plus every exit verb and same-team negative controls. ⛔⛔ **CORRECTED IN PLACE + CLOSED 2026-08-20 (owner, reported form): the ruling above OVERSTATES the owner's intent — B234 was meant UI-ONLY, and the cross-transport console/BLE expansion is WITHDRAWN** (a partial implementation was dispatched on the row's wording, stopped, and surgically reverted the same day; the tree carries none of it). **On the UI the guard ALREADY EXISTS and is measured:** slice 5's `create_confirm` opens with **BACK selected** and shows the **`REPLACES <fp>`** warning whenever a create would displace a non-zero team (QG-passed; metal-checked, walkthrough step 30 — BACK proven zero-transaction on air), and **no OLED flow for `team 0`/`leave` exists at all**. ⇒ nothing to build; **closed as satisfied by §UI-15 slice 5.** ⓘ If a console/BLE `confirm` guard is ever wanted, that is a NEW ruling and a new row — ⛔ do not resurrect this one's cross-transport text as authority. |
| B235 | ✅ **CLOSED 2026-08-21 (same day) / GATE INTEGRITY / QG-VERIFIED** | ⛔ **THE MUTATION HARNESS SILENTLY IGNORES UNKNOWN FLAGS.** `python3 tools/probe_ui_model_mutations.py --bogus --where` ⇒ exit 0, runs `target=model` — the arg parsing (`:88` area) validates `--target=` VALUES but never rejects an unrecognised flag. ⇒ **a typo like `--taret=config` runs the MODEL battery and mutates the WRONG source**, reporting a green verdict about a target the operator never asked for — the same silent-mismeasurement family as [[B217]], arriving through the front door. **Required: unknown arguments REFUSE loudly (exit non-zero, name the flag, list the known ones); a control proves the refusal.** Found by QG during the B217 re-gate; kept a SEPARATE row per QG's C1 note. ✅ **CLOSED 2026-08-21, QG-VERIFIED:** unknown `--bogus` and the mistyped `--taret=config` refuse loudly with **exit 7**; an unknown `--target=` value refuses with **exit 6**; multiple positional filters refuse; every legitimate form still works (`--target=config --where` verified). ⓘ The sibling zero-match-prefix hole (`ZZZ` ⇒ 0 mutations, exit 0) was found in the SAME re-gate and stays with [[B217]]'s open correction. |
| B236 | **OPEN / LOW / DECLARED BEHAVIOURAL COST of ruling R-1 / 2026-08-21 — bench observation owed** | ⚠ **THE NORMAL SEND SLOT IS NOW HELD UNTIL THE OPERATOR ACKNOWLEDGES A COMPOSE RESULT.** A §UI-17 S2 consequence, stated by the coder in `src/firmware_ui_send.h` and twice flagged upward: `ui_pump_trackers` binds the one normal send slot to `compose_open()`, and under R-1 a blank no longer frees it — the slot is held for as long as the result view stands. **Bounded by attention** (one wake press + one acknowledgement) and ⛔ **the EMERGENCY slot is SEPARATE** — a forgotten compose can never block an alarm. The gain is R-1's point: the ~36 s `channel_sent` / 60 s e2e verdicts the old code discarded as "abandoned" are now the ones most likely to be seen. ⓘ NOT a defect — a ruled trade recorded so the metal test observes it knowingly. **CLOSE BY:** the §UI-17 full-UI metal session confirming the bound feels right in the field (or the owner ruling a release bound if it does not). |
| B237 | ✅ **CLOSED 2026-08-21 (same day) / GATE INTEGRITY / QG-VERIFIED** | ⛔⛔ **THE FEATURE-PROBE RUNNER COUNTS A CRASH AS A SUCCESSFUL RED CONTROL.** Measured: control **C81's mutant SEGFAULTS (signal 139) with ZERO failed checks, and the runner reports it VERIFIED** — `run.sh:327` accepts ANY non-zero exit as "the control reddened", so a mutant that dies before asserting anything is indistinguishable from one the probe caught. Immediate trigger: a null-unsafe `strchr()` in P18 (`probe_main.cpp:2765`) under C81's mutation (`run.sh:731`) — but **the CLASS is the runner's exit-code laundering**, the same family as [[B227]]/[[B229]]/[[B235]]: the gate's own plumbing turning "did not measure" into "measured RED". **Required (QG-ruled):** P18 null-safe · the runner accepts ONLY a normal assertion-failure exit WITH ≥1 reported `FAIL` line · signals/abnormal exits/zero-failed-checks ⇒ unusable/failing, never verified · full probe re-run with no segfault output. Plus the stale inventories: C105-C111 → C112 included; TEAM says 12 mutations, has 16; STATUS says 12, has 13. ✅ **CLOSED 2026-08-21, QG-VERIFIED:** P18 made null-safe via a FAIL-CLOSED reader (`body_row_unmarked` — a missing row answers `false`, ⛔ never "carries no marker"; the one unsafe site swept and fixed); the runner classifies **four verdicts** (`red` only on a normal assertion-failure exit WITH ≥1 named `FAIL`; `passes`/`abnormal`/`silent` all gate-failing, the crash notice CAPTURED not printed), grounded in the probe's own binary exit contract; the **fourth worthlessness check** added to the header; **the fix carries its own repro**: a real SIGSEGV control classifies UNUSABLE while the anti-overcorrection arm proves a genuine 1-with-failures stays RED. **MEASURED (QG independent):** C81 went from a crash scored "RED (0 checks failed)" to a GENUINE **29-check reddening** (the null-safety fix made it a real measurement — the only reclassification, honest not tuned); full probe zero segfaults, 135 controls / 0 unusable; native unmoved 1893/87517/0. Inventories corrected (incl. a fourth stale count QG found at `run.sh:963`). |
| B238 | **OPEN / LOW / PROBE-HARNESS CLOCK-WRAP TRAP / found §UI-17 S5, 2026-08-21 — documented in-source, workaround in place** | ⚠ **THE FEATURE PROBE'S `walk_to_slot` STEPS THE CLOCK BACKWARDS ~90 ms PER WALK, WHICH `ArduinoClock::accumulate_millis_wrap` READS AS A MILLIS WRAP AND ANSWERS WITH +2^32 ms.** (`leave_list` returns after its own `paint(t)`; the caller paints at the same `t`.) A long probe run crosses **137 such phantom epochs**, so any node-side timestamp taken BEFORE a walk is ~49 days old AFTER it — S5's first P19 cut failed exactly that way (labels dropped past `team_key_of_id`'s 48 h gate, `id 84 … 49d` on the panel). ⓘ **Harness-only; shipped firmware is untouched** (real `millis()` never steps back). **Workaround, in place and written out in-source at P19: walk FIRST, stamp SECOND, never walk between a stamp and the frame.** The shared helper was deliberately NOT changed mid-slice (C1). **CLOSE BY:** a small harness slice making the probe clock monotonic across `walk_to_slot` (or wrap-proofing the helper), with a control proving a stamp survives a walk — until then every new probe phase must follow the stated discipline. |
| B239 | ✅ **CLOSED IN-SLICE 2026-08-22 (§UI-17 S8 — found by the coder's own probe BEFORE landing) / DEADLINE-WRAP REVIVAL CLASS** | ⚠ **A WRAP-SAFE `now < deadline` ON A DEADLINE THAT IS NEVER CLEARED REVIVES ~24.8 DAYS AFTER IT EXPIRED** (once `now` runs 2^31 ms past it, the signed-wrap comparison reads the stale deadline as future again — for the NEXT ~24.8 days). S8's first cut armed `_msg_wake_until_ms` and never cleared it ⇒ a node that received ONE message would stop blanking 24.8 days later — the F-10 power regression arriving from the CLOCK instead of the traffic. ⓘ `hold_active` carries the same bare comparison and is immune ONLY because its `retained` term clears — **the immunity is the CLEARING TERM, not the comparison**. Found because the probe's backwards time jump made a stale deadline look live (P20a failed); **fixed by BOUNDING the window** (`left != 0 && left <= kBlankMs`), pinned by a 37-days-later case + mutation **S17**, with a fourth uptime base covering the one blank the bound alone cannot see. ★ **THE STANDING LESSON for every deadline in this codebase: a wrap-safe comparison over a deadline needs either a CLEARING term or a WINDOW BOUND — audit any new `now < deadline` for which one it has.** |
| B240 | ✅ **CLOSED 2026-08-22 (same day) / QG-PASSED after one 3-blocker round · ⛔ UNCOMMITTED, metal = the drafted keyring bench part** | ⛔⛔ **THE SEALED `team grantkey` RECEIVE SIDE ADOPTS THE KEY TO RAM ONLY — A REBOOT LOSES IT.** `team_key_grant_receive` (`lib/core`) adopts live and pushes; the firmware's push handler (`src/fw_main.cpp:1470-1474`) only PRINTS; the sole writers of `Blob::team_ch_*` are reached exclusively via `seed_blob_from_live` on OTHER verbs' save paths — **no path persists a key that arrived by grant.** ⇒ a member who received the team key over the air can read the encrypted channel until the first power-cycle, then silently cannot — the worst shape for a hiking safety device (works at the trailhead, dead at the summit), and design §3.6.4 point 6's "durable adoption" is FALSE today. ⓘ Pre-existing (the mobile-arc key-forward slice landed the transport, not the persistence); found by the UI-16 spec's F-6 inventory. **PLANNED FIX — RE-RULED BY OWNER 2026-08-22: a dedicated `/mrteams` KEYRING, not a small `src/` patch** — a new indexed NV record (`TeamKeyBlob`: magic/version/count + 4 × `{team_id, team_ch_pub, team_ch_priv, reserved}` — four entries matching the four join profiles, ~296 B flash, no permanent Node RAM growth), with the owner's policy set (one record per team_id · atomic replace · identical-material writes nothing · a FULL keyring fails LOUDLY, never silently evicts a secret · factory reset erases it · no labels · secret buffers wiped · boot install only on exact active-team match) and the RETENTION/ACTIVATION rule: keys are RETAINED on leave but ⛔ NEVER silently reactivated by mere knowledge of the public team_id — `/mrcfg` gains an active-binding fact and re-joining a known team offers `SAVED KEY FOUND` / explicit `USE SAVED KEY` (BACK default). Persistence-FIRST on grant receive (only `saved` reaches the UI; a failed save reports `TEAM KEY ACTIVE` / `NOT SAVED — LOST ON REBOOT`). Owed its own metal power-cut part. Sequenced FIRST in the UI-16 arc; ⛔ still must not wait if UI-16 defers. ✅ **CLOSED 2026-08-22, QG PASS:** the `/mrteams` keyring landed as ruled (K1+K2 — 296-byte record measured, four-state read, counted zero-write coalescing, loud-full refusal through the whole transaction, retained-never-reactivated, `/mrcfg` v24 active binding — an NV version, not wire). ⚠ **QG's first gate found THREE boot/transaction holes, all one family — the keyring's verdict was not the sole authority over the live key:** the legacy `/mrcfg` boot install ran FIRST (a failed restore left the old key live) · a FAILED same-team re-key became effective after reboot (keyring written before the failed `/mrcfg` save, binding still active) · membership was missing from the exact match (a stale binding could install another team's key). **Fixed structurally:** the legacy install REMOVED (`/mrcfg`'s pub demoted to the COMMITTED WITNESS), a **FIVE-TERM restore predicate** (active · membership==binding · record present · keyring pub == committed pub · derived pub == stored priv — no new NV field), and every refusing arm exits through ONE `refuse()` funnel that clears the live key. Both boot arms proven RED against the pre-fix code first. **MEASURED:** native 1967/88323/0 · `teamkeyring` 25 RED / `provservice` 10 / `devicenv` 19, all 0 unusable · both probes green (147 + 46 controls). The B240 falsifier: native P05 + the bench part's power-cycle step. |
| B241 | **OPEN / MEDIUM / OUT-OF-BOUNDS READ, pre-existing — found §UI-16 N4, 2026-08-23; METAL-CONFIRMED 2026-08-26** | ⚠ **`label_from_hash` CAN HAND ITS `%s` CONSUMERS AN UNTERMINATED BUFFER.** `Node::peer_name_find` (`lib/core/node_hashlocate.cpp:450-457`) copies `min(name_len, cap)` bytes and returns the count — it ⛔ does **not** NUL-terminate; the peer name cap is **32** while every UI label buffer is `char label[kLabelCap + 1]` = **15** (`src/firmware_ui_model.h`). **METAL REPRODUCTION:** after naming two Heltec nodes `H1` and `H2`, the mobile TEAM/member display intermittently shows garbage characters after the valid name. This proves the defect is not restricted to overlong names: a short successful copy also leaves its following byte untouched, so stale non-zero tail bytes become a visible suffix; a name ≥ 15 additionally guarantees the existing out-of-bounds-read shape. `label_from_hash` (`src/firmware_ui.cpp:437-438`) only `snprintf`-overwrites on a **0** return, so its `%s`/`strlen` consumers receive no termination guarantee. ⓘ N4's invite-member projection terminates explicitly (`src/firmware_ui.cpp:744-755`) and says why; the shared helper remains the unfixed authority. **FIX SHAPE:** terminate in ONE place — preferably make `peer_name_find` guarantee NUL termination whenever `out != nullptr && cap > 0`, with the returned length excluding the terminator; otherwise terminate centrally in `label_from_hash`. Tests must poison the destination tail, copy exact short names `H1`/`H2`, and prove the next byte is NUL; retain the 32-char-to-15-byte clamp/OOB case. Do not add renderer-specific clearing. **CLOSE BY:** a small dedicated slice (a `lib/core` contract change carries corpus obligations). |
| B242 | **OPEN / LOW / DECLARED PRODUCT BOUND — found §UI-16 N4, 2026-08-23** | ⚠ **THE INVITE WINDOW SEES AT MOST `kMaxTeamRows` (8) MEMBERS** — the candidate projection inherits the TEAM screen's bound, so on a team whose member count exceeds it a ninth member can never be offered a key through the on-device window (serial `team grantkey` is unaffected). Stated in-source at the projection. ⓘ NOT a defect today (bench teams are ≤ 3); recorded so the bound is chosen deliberately rather than inherited silently. **CLOSE BY:** an owner ruling — either accept 8 as the device-UI bound (close as policy) or size the invite projection independently of the TEAM screen's row cap. |
| B243 | ✅ **CLOSED 2026-08-25 (§UI-16 K3+K4b, two QG rounds) / QG-PASSED** | ⚠ **THE RULED FAILED-SAVE WORDING (`TEAM KEY ACTIVE` / `NOT SAVED — LOST ON REBOOT`) IS IMPLEMENTED BUT DEVICE-UNREACHABLE.** `lib/hal/mr_ui.h` declares the ONE UI door (`mr_ui_on_push`), and F-10 rules a failed persist must ⛔ NOT forward the push through it — so the failure verdict has no door: on device a failed grant-receive save is **SILENT on the panel** (⛔ never a false `TEAM KEY RECEIVED` — the safety half holds; the honesty half is missing). The failure arm, both ruled sentences and every negative property are landed and natively driven; the gap is stated in-source at `UiModel::on_team_key_note` per [[mark-done-vs-missing-in-code]]. **FIX SHAPE (small, stated by the K3 coder):** one hook beside the six in `lib/hal/mr_ui.h` + one call in the drain loop's `else` (`src/fw_main.cpp`) — a `lib/hal` touch, so the slice carries the corpus argument (hal is sim-inert but argue it). **CLOSE BY:** a dedicated small slice; bench §7.5's forced-save-failure step stays OWED until it lands (noted in Part 42). ⚠ **UPDATE 2026-08-25 (K3+K4b round 1): the second door LANDED** (`mr_ui_on_team_key_unsaved`, `lib/hal/mr_ui.h` — the sim-inertness argument QG-confirmed, both boards green) **but QG HELD the row OPEN on a classification defect the wire introduced:** `team_key_grant_persist()` collapses `GrantSave` to a boolean, so the pre-live-key refusals (`zero_team` / `not_our_team` / `no_live_key` — the handling-time race checks) also reach the unsaved door and display **`TEAM KEY ACTIVE`, which is FALSE there** (nothing is active). QG-ruled fix in flight: a three-way routing verdict across the config seam — `received` (forward) / `active_unsaved` (the ruled wording, true only AFTER the live-key check) / `suppressed` (neither door; the panel says nothing) — decision in the service layer, `fw_main` routes only. **CLOSE BY:** the K3+K4b round-2 recheck. ✅ **CLOSED 2026-08-25, QG-PASSED (round 2):** the second door landed AND the classification defect the first wire introduced was fixed as QG ruled — the seam returns the three-way **`GrantUiRoute`** (`received` / `active_unsaved` / `suppressed`), classified purely in `grant_ui_route_of` with the STRUCTURAL dividing line (an arm's position relative to re-check (3): past it the key is live by control flow, so `TEAM KEY ACTIVE` is true there and only there); the three race arms are `suppressed` (neither door — the panel says nothing, the console still prints); `fw_main` routes a `default`-less switch (U3); `lib/hal/mr_ui.h` = the one argument-free hook. Blocker mutations T39/T40/T41 + W47 ctl 7 RED; both probes exit 0; native 2084/91382/0; boards green (QG). ⚠ Residues, both bench-only by ruling: the forced real-NV-failure arm = **Part 42** (metal check, ⛔ not a software blocker — owner 2026-08-25); `binding_failed` on metal = not provokable by hand, host-gated by T29. |
| B244 | ✅ **CLOSED 2026-08-25 (§UI-16 K3+K4b) / QG-VERIFIED** | ⚠ **`tools/probe_board_ui` W7 and W9 FAIL PRE-EXISTING** — their regexes went stale against the §UI-17 S1/S3 reworks (measured, not argued: the K3 diff to `src/firmware_ui.cpp` is +26 lines touching zero of `ui_have_fix`/`ui_perform_send`/`team_pick_gone`/`TEAMMATE GONE`). Until re-anchored, the wiring probe's summary carries 2 standing FAILs that every future slice must explain away — the normalization-of-red shape. **CLOSE BY:** a small harness re-anchor (W7/W9 re-pinned to the as-built §UI-17 text at match count 1), fair game to bundle with the next probe_board_ui-touching slice. ✅ **CLOSED 2026-08-25, QG-VERIFIED:** W7/W9 re-anchored to the as-built §UI-17 text (one stale clause each, measured; W7 → the pure `ui_status_have_fix` call, W9 → the collapsed marker predicate `entered && !st.team_pick_gone && idx == st.cursor`), match count 1 each, nothing weakened, one EXTRA control added where the re-anchor exposed an unseen harm. `probe_board_ui` exits 0 at **54/54 wiring / 164 controls RED** — six controls previously unreachable behind the standing FAILs now run. |
| B245 | ✅ **CLOSED 2026-08-25 (§UI-16 K7) / QG-PASSED** | ⚠ **A MEMBER WHO JOINS BEFORE THE INVITE WINDOW OPENS CAN NEVER BE GRANTED THE KEY FROM THE PANEL.** The ruled F-11 diff defines a candidate as *first observed after the window's opening snapshot* (N4 pin 2: present-at-snapshot ⇒ never a candidate) — so H1 create → H2 joins via NEARBY → H1 opens INVITE MEMBER shows H2 nowhere, and the only grant path is the serial console. Structural, not an implementation bug: the intended choreography (window first, joiner second) is inverted by the natural race, and no honest "keyless" predicate exists (the wire deliberately does not attest key possession). **Options presented 2026-08-25:** (1) an operator-initiated per-member GRANT from the TEAM screen's roster, reusing the landed N5/N6 machinery verbatim (preflight → REQUEST PUBKEY → GRANT KEY w/ safe default, full hash, send_aired correlation) — P-12 and the F-11 window semantics untouched; also covers re-grant-after-reflash — **recommended**; (2) widen the invite diff by cached-pubkey absence — a proxy wrong in both directions, re-litigates F-11 — advised against; (3) snapshot at team creation — makes volatile state persistent (against F-13), ages badly. ★ **RULED (owner, 2026-08-25): OPTION 1** — the per-member GRANT from the TEAM roster; spec §K7 drafted, slice queued behind K6's gate. ✅ **CLOSED 2026-08-25, QG-PASSED (combined K6+K7 gate):** the roster grant landed as ruled — the act on the entered-TEAM member row's DM sub-view (identity FROZEN at entry: `compose_grant_hash` beside `compose_peer`), opening the landed N5/N6 chain verbatim; keyless/self/unresolved/route-only all HIDE the act; the B245 repro is pin 2's end-to-end case, green; the invite window's diff is byte-empty. QG-verified: grants from the frozen authoritative hash never a cursor/label/node-id; no auto-grant of newly observed members. Metal residue = bench Part 45 (the real TxDone edge). |
| B246 | **OPEN / LOW / GATE BLINDSPOT + a cheap standing check on offer — found §UI-10/11 P3 QG round, 2026-08-26** | ⚠ **THE BOARD-ABI COST OF A MODEL FIELD IS INVISIBLE TO EVERY NATIVE GATE.** Measured: P3's `compose_gen` (u32) cost **0 on the host** (the 8-byte adapter pointers open a 4-byte hole that swallows it) but **+8 B on xtensa** (4-byte pointers, no hole) — ×2 resident `UiState` instances = **+16 B of board RAM no native `sizeof` case, battery or probe could see**; only the per-board `RAM_used` diff caught the discrepancy (QG's +336 vs the claimed +328). D2's native-alignment warning paying out exactly as written, now also recorded at `UiState::compose_gen` and both resources cases. **THE OFFER:** the P3 coder built a 6-line xtensa `sizeof`-probe (the env's own `toolchain-xtensa-esp-elf`, compile-only, no link) that measures board-ABI struct sizes host-side — cheap enough to be a STANDING check in the resources discipline. **CLOSE BY:** owner ruling — adopt the probe as a standing harness check (a small slice wiring it beside the offsetof cases), or accept per-board `RAM_used` as the only guard and close as policy. |
| B247 | ✅ **FIXED 2026-08-26 / QG PENDING / ⛔ UNCOMMITTED — separate micro-slice after V4-1** | ⚠ **THE CLEAN OLED WARNING CENSUS WAS RED BEFORE V4-1:** its live pins are `gateway_heltec=174`, `heltec_mobile=178`, `heltec_v3=178`, but clean `HEAD f3ecec1` measured **175/179/179**, with the same one added `-Wformat-truncation` diagnostic in every environment at `src/firmware_ui_join.h`'s `join_fmt_phy()` `snprintf`. An isolated clean-HEAD rebuild and the V4-1 tree produced the identical warning set; `-Wswitch` remained **0**, so V4-1 did not own the warning (C1). ✅ **FIX:** `join_profile_phy_valid()` composes through the store's existing `join_request_from_profile()` and asks the transaction's existing `validate_join()` — one domain authority, no duplicate range table. A structurally readable slot with an out-of-domain layer/SF/frequency/bandwidth now renders one bounded **`PROFILE INVALID`** line and no bogus frequency; legal lower/upper boundaries remain byte-exact. The public PHY buffer remains **19 bytes**; a private **24-byte** scratch is derived from the raw field types solely so GCC can prove its write, then the validated line is bounded-copied. **REGRESSION:** 2 new B247 cases / 45 assertions cover every low/high edge, `UINT32_MAX`, the exact valid boundaries, and a small-buffer sentinel; J26 restores the unvalidated path and turns **28 assertions RED**. Full `uijoin`: **26/26 RED, 0 unusable**, including re-anchored J22. **GATE:** native **2213 / 95276 / 0**; warning census restored at the unchanged pins **174 / 178 / 178**, zero `-Wswitch`; board probe V3 **124/0**, V4 **110/0**, all controls RED; firmware probe l2 **426/0**, V3 **856/0**, BLE **426/0**, **220 controls / 0 unusable**; V4-1 board pair green (`heltec_v3` **219380 RAM / 1329748 flash**, `xiao_sx1262` **171332 / 564556**); s18 exact **`9868cad3` / 269905 / 0** after a zero-action simulator rebuild; anchor-table diff empty; `sizeof(Node)` **222008**, `sizeof(TxOutcome)` **24**, timers/wire/NV unchanged; `git diff --check` clean. Awaiting independent QG before owner commit. |
| B248 | ✅ **FIXED + METAL-PASSED ON V4.2/V4.3 2026-08-26 / QG PENDING / UNCOMMITTED — V4 panel-power polarity** | On a metal V4.3 identified by the runtime authority `fem=kct8103l`, the provisional V4 `MR_UI_VEXT_ON_LEVEL=HIGH` produced exactly `!! OLED panel did not ACK (check Vext / addr 0x3C / wiring)`. Radio initialization remained healthy (`radiohw=1 rfcfg=1 rfok=1`), separating this from the RF port. Heltec's own V4 factory test implements `VextON()` as GPIO36 LOW, agreeing with the V4.2 datasheet and contradicting the pinned MeshCore HIGH claim. **FIX + POSITIVE CONTROLS:** the shared V4 trait is LOW; the same image makes the OLED work on both V4.3 (`kct8103l`) and V4.2 (`gc1109`), so no revision-dependent GPIO logic is needed. T13 restores HIGH and turns the board-canvas probe RED. Bench Part 47.8 retains the separate page cadence, blank/wake, current and battery checks rather than claiming them from panel illumination alone. |
| B249 | ✅ **CLOSED 2026-08-26 / OWNER METAL-PASSED — UI-16 DISCOVERABILITY** | ⚠ **`INVITE MEMBER` DID NOT ANNOUNCE THE TEAM, SO A NEARBY TEAMLESS MOBILE COULD BE UNABLE TO DISCOVER IT UNTIL THE NEXT FIVE-MINUTE STEADY TEAM BEACON.** Metal reproduction: two mobiles had matching radio PHY; the long-running member opened `INVITE MEMBER`, but its debug log showed no TX and the joiner could not discover/join through the panel. Entering `team <team_id>` manually on the joiner succeeded, isolating the defect to passive discovery rather than the membership transaction. This was the then-current UI-16 design, not an accidental missing call: N4 pinned opening/holding the invite window as zero-TX, while `NodeConfig::team_beacon_period_ms` defaults to 300000 ms and the NEARBY list is frozen per entry. **OWNER-RULED FIX SHAPE (2026-08-26):** opening a fresh `INVITE MEMBER` window requests exactly ONE bounded team announcement through the existing `Node::schedule_triggered_beacon()` authority. ⛔ Do not call `team_dad_fire()` (must not re-pick the team-local id or restart DAD), invent a wire command, send a key/query/DM/channel frame, or transmit on redraw/tick/wake/close. Take the opening member snapshot BEFORE requesting the announcement; keep the same-PHY + same-leaf requirement for a teamless listener. The existing scheduler supplies jitter, coalescing, minimum interval and duty/LBT handling. **SPEC CORRECTION COMPLETED:** UI-16 N4 now says “one triggered team-beacon request on window open; zero further traffic while holding, refreshing, waking or closing.” **PINS:** one request per fresh open; zero on redraw/wake/refresh/close and on teamless/gateway builds; no DAD/local-id/key mutation; announcement remains the existing public team-id beacon; mutation controls for missing, repeated and wrongly ordered requests. ✅ **IMPLEMENTED:** `IUiInviteDevice` gained one policy-free announcement request; the sole fresh-open transition takes the snapshot, establishes the window, then calls it; production `DeviceInvite` forwards exactly to `g_node.schedule_triggered_beacon()`. No UI latch, timer, retry, wire carrier or core behavior was added. **CODER GATE (independent QG not yet claimed):** native **2213 / 95296 / 0**; full model battery **223/223 RED, 0 unusable** (B249 deletion/duplicate/reversed-order = 18/12/6 failed assertions); feature probe **858/0 mobile + 426/0 layered**, **221 controls RED / 0 unusable**, including `team_dad_fire()` substitution RED by 98 checks; board probe V3 **124/0**, V4 **110/0**, traits **14/0**, structural **23/0**, wiring **59/0 with 184 controls RED**, W53 independently rejects deletion and wrong destination; essential builds `heltec_v4_mobile` **219188 RAM / 1323524 flash** and `gateway_heltec_v4` **244452 / 1273792**, both **0 RAM / +24 flash** vs the pre-change tree, warnings unchanged at **183/179**, zero `-Wswitch`; rebuilt s18 exact **`9868cad3` / 269905 / 0**. ✅ **OWNER METAL CLOSE (2026-08-26):** the first B249 metal tests passed; the owner directed closure. Part 38 step 1 records the RF/metal result. |
| B250 | ✅ **FIXED 2026-08-26 / QG-PASSED 2026-08-27 / OWNER METAL RECHECK PENDING / ⛔ UNCOMMITTED — UI-16 K7 NAVIGATION** | ⚠ **A KEY GRANT STARTED FROM THE TEAM ROSTER ENDED IN THE `PROVISION` MENU INSTEAD OF RETURNING TO THE TEAM MEMBERS.** Metal reproduction: M1 entered TEAM, selected M2, chose `GRANT KEY`, completed the required pubkey request and then the grant; after acknowledging the outcome the panel landed in PROVISION. The grant itself succeeded. **CAUSE CONFIRMED IN CODE:** K7 reused N5/N6's `Provision::*` grant chain without retaining a caller/return origin. `Provision::invite_result` unconditionally entered `Provision::menu`; NEED-PUBKEY, WAITING, BACK and REJECT likewise used invitation-window landings. `run_roster_grant()` retired the TEAM list/pick, so the parent could not be recovered honestly. **OWNER-RULED FIX:** carry an explicit typed grant origin/return context (`invite_window` versus `team_roster`) through the reused chain; never infer it from `InviteWindow::taken`, a zero id/hash or a screen arm. Invitation-origin behavior stays unchanged. Roster cancel/back/reject/completed-result exits return to the entered TEAM roster by saved TEAM-local identity, following a reordered member and refusing a gone member through the existing B64 authority. A matching roster-origin pubkey arrival opens the existing REJECT-default `GRANT KEY` confirmation and cannot send without the existing short-then-double confirmation. A terminal acknowledgement sends nothing and cannot repeat the grant. **IMPLEMENTED:** one private typed `GrantReturn`, one exhaustive five-arm lifetime classifier and one copy/consume-before-transition landing authority; `GrantOrigin::none` fails closed to PROVISION; unrelated provisioning close and emergency pre-emption retire the context. The grant decisions, screens, words, frozen hash, request/send path and outcome mapping remain shared. UI-16 K7 was corrected accordingly. **SOFTWARE GATE / INDEPENDENT QG PASS:** native **2218 / 95587 / 0**; complete model battery **239/239 RED, 0 unusable**; firmware probe layered **426/426**, child/mobile **858/858**, BLE **426/426**, **221 controls RED / 0 unusable**; `sizeof(UiState)` **504 → 504**, `sizeof(UiModel)` host **928 → 928**, ESP32-S3 **912 → 912**; essential builds `heltec_v4_mobile` **219188 RAM / 1323684 flash** (**0 / +160** vs B249) then `gateway_heltec_v4` **244452 / 1273956** (**0 / +164**); warning census **174/179/178/178/183/183**, zero `-Wswitch`; `lus` byte-identical at **`0876d3e528abf23b2e6b3f69155e179b`**; s18 exact **`9868cad3` / 269905 / 0**; corpus anchors untouched; diff check clean. No core, HAL, wire, routing, key, NV, timer, app push, UI lexeme or simulator-outcome change. **METAL CLOSE REMAINS:** complete a roster-origin grant to M2, acknowledge it, and verify the entered TEAM roster returns with M2 selected; if M2 disappeared, require `TEAMMATE GONE` with no neighbouring member selected. |
## Parked, optional, or trigger-gated

| ID | Status | Trigger or reason |
|---|---|---|
| B5 | PARKED | `channel_pull` lacks team scope and exposes static source id. |
| B6 | PARKED | Team plane lacks budget-penalty mirror. |
| B7 | PARKED | Team plane lacks slow-reprobe state. |
| B8 | PARKED / UNMEASURED | Relay behaviour for `src == 0` unresolved. |
| B9 | PARKED / SIM TELEMETRY | Team `rt_update.slot` mislabelled. |
| B10 | PARKED / TEST DEBT | Simulator lacks working `routes` command. |
| B11 | PARKED / TRACE DEBT | Frame-trace switches omit live values. |
| B12 | PARKED / REFACTOR | Seal-or-refuse logic triplicated. |
| B13 | PARKED / REFACTOR | Team-liveness scan duplicated. |
| B14 | PARKED / COMMENT | Routing comment in `node.h` drifted. |
| B15 | PARKED / REDESIGN DEPENDENCY | Binary config TLV omits `team_ch_key`. |
| B16 | PARKED / SIM PARITY | `send_layer` grammar differs between simulation and metal. |
| B18 | PARKED / D2 | Team H relay can read static binding table. |
| B19 | PARKED / FOLD INTO B12 | `deleg_ack_put` duplication; never take alone. |
| B23 | PARKED / POLICY | Metal resolve reaches AUTO and carries dead field. |
| B37 | PARKED / DEPLOYMENT TRIGGER | Symbolic wire tests cannot detect deployed-format drift. |
| B57 | PARKED / DELIBERATE | Beacon-learned binding does not consume pending `reqpubkey`. |
| B59 | PARKED / DO NOT DISPATCH | Reliable repair may require routing/custody redesign. |
| B116 | PARKED / OWNER-RULED | Channel HAVE is not delivery evidence. |
| B118 | RECORD ONLY | REQUIRES ANSWER app-code direction remains unimplemented; authentication design is prerequisite. |
| B152 | PARKED / OPTIONAL | Two airtime-free link-confidence refresh hooks remain unwired. |
| B154 | PARKED / OPTIONAL | Seconds-until-next-mobile-retry needs state or HAL plumbing. |
| B191 | PARKED / PRODUCT LIMIT | OLED can delete only newest-four-per-kind browsable messages. |

## Non-bug decisions and audits

| ID | Status | Decision |
|---|---|---|
| D1 | TRIGGER-GATED | Revisit team DV hop cap only when a team path exceeds eight combined hops. |
| D2 | OPEN AUDIT | Audit plane-typed reads that can fall back to static table. |
| O2 | PARKED | Fold `deleg_ack_put` de-duplication into B12. |
| O4 | OPEN SECURITY DECISION | Decide protection for BLE `team exportkey` before broad provisioning exposure. |

## Dispositions applied by this cleanup

- **Fixed/closed and archived:** B65, B70, B99, B114, B187, B188, B192, B194, B195, B197, B198, B200, B202.
- **Consolidated; no separate dispatch:** B155 (maintenance), B163 (superseded by B182), B183 (split into
  B186/B187), B189 (hardware path done; remaining recovery is B186b).
- **Record-only evidence removed from the work queue:** B63, B77, B82, B86, B89, B98, B144, B150, B190, B201,
  B203.

Exact pre-cleanup wording and evidence remains in the dated snapshot. This is a disposition index, not a deletion.
