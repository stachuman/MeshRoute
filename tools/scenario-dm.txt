# MeshRoute lab — REALISTIC traffic (meshroute_lab.py run tools/scenario-realistic.txt [-v])
# Each node INDEPENDENTLY Poisson-generates a message STREAM over the window — per message a DM (to a uniform-random
# other node) or a channel broadcast, each at its OWN arrival time. The superposition of 8 independent streams is
# real mesh traffic: NO synchronized "1 DM + 1 CH per node", NO "DM then CH 0.3 s later". (Contrast scenario-oracle.txt,
# the deterministic round-robin probe used to validate the oracle machinery.)
workload: realistic
duration_s: 120                 # each node emits messages across this window
rate_per_min: 3                 
dm_fraction: 1
channel: 0
ack: true                       # -a on the DMs (drives the e2e-acked criterion)
enc: false
seed: 0                         # reproducible; change to resample the stream
settle_s: 30                    # after the LAST scheduled message
eventual_s: 100                 # > duration + settle + repair — late messages still get a fair eventual window
